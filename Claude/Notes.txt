Claude code should not read or modify this file.


QQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQ
In the local folder is a C# solution 'TaskMan.sln'.  This loads other C# projects.
  All this code is quite old - what needs to be done to move it onto the latest relevant .Net framework?
AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA

Current state

- TaskMan.sln (VS2013 format) + 5 project references: TaskMan (the exe), Utils, PlanDisplay, DBTaskMan, DBAccess, CustomGUIControls.
- TaskMan.exe is a hybrid WinForms + WPF app (WinForms Forms host WPF UserControls via WindowsFormsIntegration/ElementHost).
- COM interop: Utils/DocumentSearch.cs actively drives Word via Microsoft.Office.Interop.Word (embedded PIA). 
- No packages.config anywhere — every reference is a GAC/framework/COM assembly ref, no NuGet packages to fight with.
- ClickOnce publish is configured (System.Deployment, BootstrapperPackages, publish URLs pointing at \\tatooine) — that whole mechanism changes shape outside .NET Framework.

What moving to modern .NET involves

Since this is Windows-only desktop (WinForms+WPF+COM), the realistic target is .NET 8 (LTS) with the Windows Desktop workload, not .NET Framework 4.8 (which is EOL-of-the-road, no further evolution) and not a cross-platform target (WPF/WinForms/COM interop tie it to Windows anyway).

1. Convert all 6 .csproj files to SDK-style, targeting net8.0-windows, with <UseWindowsForms> / <UseWPF> set as needed. The dotnet-upgrade-assistant tool can automate most of this mechanically.
3. Re-resolve COM interop for Word — SDK-style projects support <COMReference> under net8.0-windows, but embedded interop types need regenerating; alternative is switching to the Microsoft.Office.Interop.Word NuGet PIA package.
4. Fix the Security.dll reference — either bring Libs/Security into the solution as a real ProjectReference and retarget it too, or rebuild/retarget that DLL for net8.0 independently. Right now it's a loose binary dependency outside source control's build graph.
5. Replace System.DirectoryServices/AccountManagement framework refs with their NuGet package equivalents (still Windows-only, still supported on net8.0-windows).
6. Decide on a deployment story to replace ClickOnce/System.Deployment — .NET has ClickOnce support again since .NET 6, but the publish profile mechanics differ from the old bootstrapper items; MSIX is the modern alternative if you want to reconsider.
7. Rationalize build configs/platforms (drop Win32/Mixed Platforms, decide if x86 is still needed — likely only if Office interop forces 32-bit).
8. Recompile and fix the inevitable small API breaks (obsoletions, C# language version jumps to 12 automatically under net8.0 SDK).
9. Clean up: delete the stray TaskMan - Copy.sln.

The COM interop (Office + Security.dll dependency resolution) is the part with real risk/unknowns; everything else is mechanical.

Want me to start with the mechanical SDK-style conversion (step 1) as a first PR, or do you want to resolve the Security.dll/VBIDE questions first since those affect how far the conversion can go before it actually builds?
Suggested next move: Start with the SDK-style conversion first


QQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQ

The Security DLL is built from the solution at C:\Users\Neil\source\repos\ProjectPal\Original\Libs\Security\Security\Security.sln
  I believe this is a C++ Solution
  It won't compile and gives the following error.
  "C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Microsoft\VC\v180\Microsoft.CppBuild.targets(473,5): error MSB8020: The
  build tools for Visual Studio 2013 (Platform Toolset = 'v120') cannot be found. To build using the v120 build tools, please install
  Visual Studio 2013 build tools.  Alternatively, you may upgrade to the current Visual Studio tools by selecting the Project menu or
  right-click the solution, and then selecting "Retarget solution"."
  This is old code too, which I need to work with 'TaskMan'.
  How do I update 'Security' to the latest and also so it can be used with 'TaskMan'?

AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA

Only Utils.csproj consumes it, and that's the complete source — good, this is a self-contained, low-risk piece.

  What "Security" actually is

  I read through Security.cpp/SecurityInternal.cpp — it's a C++/CLI wrapper (CLRSupport=true, "ManagedCProj") around ~100 lines of pure algorithmic code:

  - Encrypt/Decrypt — a rolling character-substitution cipher over wchar_t strings.
  - EncryptBytes/DecryptBytes — a rolling XOR cipher over a byte array.

  Critically, there is no native/unmanaged code here at all — no Win32 API calls, no P/Invoke, no pointers to anything outside the managed heap beyond the
  standard C++/CLI string-marshaling boilerplate (pin_ptr, PtrToStringChars). It's C++ syntax, but it's 100% safe managed logic. (Side note, not urgent: this
  is a fixed, keyless substitution/XOR cipher, not real cryptography — worth knowing if Encrypt/Decrypt is protecting anything you actually care about being
  unreadable.)

  Your two options

  Option A — retarget the C++/CLI project. Install the VS2022 "C++/CLI support for v143 build tools" (and, since you want latest .NET rather than .NET
  Framework, also the ".NET Core" C++/CLI component), bump PlatformToolset to v143, retarget TargetFrameworkVersion/CLRSupport to net8.0. Workable, but it's a
  heavier toolchain (full C++ workload + C++/CLI's newer, stricter .NET-Core support) for what's ultimately ~100 lines of pointer-juggling.

  Option B — port it to C# (recommended). Since none of the logic touches unmanaged memory or native APIs, it translates 1:1 into a small SDK-style C# class
  library — same namespace (Security), same class (Functions), same four method signatures — so Utils.csproj just swaps its binary hint-path reference for a
  normal ProjectReference, no call sites elsewhere change. It becomes a first-class project in the solution/build graph instead of a stray prebuilt DLL, and
  it retargets to net8.0 alongside everything else with zero extra tooling.

  I'd go with B: it's less risky, removes a whole toolchain dependency, and fixes the "DLL isn't in source control's build graph" problem at the same time.
  Want me to go ahead and write the C# Security project and wire it into TaskMan.sln as a ProjectReference from Utils?

QQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQ
Yes, create a new C# SDK-style class library for the current 'Security' and name it 'SimpleSecurity' and wire it into the current 'Utils' so it works with
  minimal changes to Utils.
  We'll need to think about moving everything to the latest .Net (not .Net Framework), but to start, just focus on 'SimpleSecurity' as a C# solution/project and getting 'Utils' to work with it.
AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA

Good — bin/ and obj/ are gitignored, so the new project's build output won't accidentally get staged. Everything is in order and I haven't committed anything (per convention, only on request).

  Summary

  New project: V1/Libs/SimpleSecurity/ — an SDK-style netstandard2.0 class library (SimpleSecurity.sln + SimpleSecurity.csproj), with Functions.cs a line-by-line C# port of the original C++/CLI
  Security::Functions class, same namespace Security { public static class Functions } shape. I verified it's byte-for-byte compatible with the original DLL by loading a prebuilt copy of the old
  Security.dll in a throwaway harness and comparing outputs — string and byte cipher results match exactly, so anything already encrypted with the old assembly still decrypts correctly.

  Wired into Utils: swapped the Security.dll hint-path <Reference> for a <ProjectReference> to SimpleSecurity. SecurityFunctions.cs — the only consumer in the whole solution — needed zero code
  changes.

  Unavoidable knock-on change: a netstandard2.0 project can't be consumed by a project targeting .NET Framework v4.0 (that pairing isn't supported at all), so Utils had to move to v4.8. That in turn
  meant every project that depends on Utils (CustomGUIControls, DBAccess, DBTaskMan, PlanDisplay, TaskMan) also needed the same one-line TargetFrameworkVersion bump, since .NET Framework refuses
  references to assemblies built against a higher version. This is just moving everyone to the top of the .NET Framework 4.x ladder (purely additive, no API removals) — not the eventual .NET 8
  migration.

  Verified: full TaskMan.sln rebuilds clean via real MSBuild (0 errors, only one pre-existing unrelated warning), producing ProjectPal.exe.

  Left alone / worth knowing about:
  - Libs/Security/ (the old C++/CLI project) and Libs/NomuraUtils/ (an unrelated stray duplicate) weren't touched — neither is part of TaskMan.sln.
  - The old Security.dll binaries still sit in various bin/ folders; harmless, and gitignored.
  - SimpleSecurity is deliberately netstandard2.0 rather than net48, so when you're ready to move Utils and the rest to real modern .NET (net8.0-windows), SimpleSecurity won't need to change at all.

  Want me to move on to the next piece of the modernization (e.g., converting Utils itself to SDK-style), or pause here?

QQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQ


Hints

/desktop – continue conversation with Calude Desktop

-----------------

Look through the old ProjectPal code and find the algorithm that defines 'Urgency'.  Add this to the KeyConcepts.md document.

Read the document 'Claude/Guidelines/document-guidelines.md' then give each of the 4 main documents a table of contents.

KeyConcepts.md is to be more than just a Glossary.  It will define key concepts, explain what they are and why they are key.  It will also elaborate on where this concepts fit into the overall solution.



---------------

A person can belong to more than one team.  In each team they may have a different role.

Let's loose the concept of a visibility flag.

You said a Task has "an optional Component classification".  Please check the code under 'V1.2' and determine if this is really optional.  Also, it isn't a 'Component classification", it should be "relates to a component"

Attachments my also be hyperlinks.

Looking at the open questions
1. For Stage 1 (see Goals.md) every Project belongs to exactly one team, this may change in the later stages.
2. Derived is the way I want to go
3. For Stage 1, there is only one user at a time.  For later stages, multiple users may use the tool at the same time and see each others updates in real time.
4. I think we need an organisation-level administrator role.  This can be an attribute attached to a Person, i.e., many people could have this and it is not team specific.
5. Yes, there is.  In this domain a resource is something that is responsible for getting something done, which means an external vendor can be a resource, however, an item of equipment is not, since it is not responsible.  However, let's leave this out of Stage 1.

Since the concept of Stages has come up, in DomainModel.md, we want to focus on Stage 1, but also record what is out of scope for Stage 1.  Please add a section for extensions to the Domain Model that will or may be needed after Stage 1.

I want to create a new doucment under 'Claude' called 'User Interface Windows'.  In it I want to list each Window type the information it contains and how a user may interact with it.  To prime this document, sweep the V1.2 code, gather all of the
C# windows that are in use and capture the information each displays and the different ways the user can interact, i.e., what information is presented (and whether it is read-only or editable) and how a use may navigate from one window to another.

----------

Take a look at DataBaseHostingOptions.md
I want you to create a plan of everything I need to do to put in place a database to support Stage 1 on my local PC, using the recommendations in DataBaseHostingOptions.md.
I want to to know what I need to download and install (and where from).
What needs to be configured.
What code needs to be written and by whom.
I'd like you to look at the DomainMondel.md and design a suitable schema, then produce the required code to implement that schema.
I'd like some notes on how to set up the database, install the schema and test it.
I'd also like some example data in the database.
If you wish and are able, you can look on this machine at the SQLServer database ProjectPalDB_1 for ideas on the data.
Please document this all and place any files/code in 'Claude/DatabaseSetUp1', we'll find a better home for it once I see what it is.


Rename the concept of 'Stages' in Goals.md do be 'Levels', update other documents and file/directory names to match


In the Level1_Implemenation folder, create a new document called 'ImplementationPlan.md' - This will be the core plan for the project in the scope of Level 1.
It will be broken down into phases.
The initial set of phases consists of the Database setup (which is done), followed by additional phases which can be taken from section 8 of InitialDatabaseSetupPlan.md.
In the Level1_Implemenation folder also create a 'Scope.md' document that will detail what is in and out of scope for this Level.  You can use 'Goals.md' to seed this.
Inside the Level1_Implemenation folder there will be additional folders, one for each phase.
Details of each phase will be documented in each subfolder, with any supporting documention.
There is already one called 'DatabaseSetup' which was the first phase.
The 'ImplementationPlan.md' will reference the documents in subfolder for details, rather than contain the details itself.
The 'ImplementationPlan.md' will track all of the phases and their status.
The 'ImplementationPlan.md' can contain open questions related to the Level as a whole, but open question specific to individual phases should be contained in
plan related documents in the phase subdirectory.

We will also need folders Level2_Implemenation and Level3_Implemenation, each of these will need their own 'ImplementationPlan.md' and 'Scope.md' documents.
These will be used as place holders intially, but things that are deferred from Level 1 can be placed in these as they arise.

We will work together on fleshing out all of the above as we proceed with the implemention.  The information will come together as the implementation evolves.

Create a new document in 'Guidelines' called 'ImplementationApproach.md' that documents the approach described above.

Then create and populate all of the directories and documents, as far as can be resonably done at this moment in time. 


This seems to be telling me that we should not use PostgREST at all.  Does this solve some of issues?

===========================

4. Minor. The Search endpoint (§2.1, §4.2) covers Task/Project/Component/Remark, but UseCases.md's Search / Find use case explicitly frames "metadata/field search" (as distinct from full-text content search) as the Level 1 baseline for Attachments too — that's silently missing.

5. Minor / worth stating explicitly. KeyConcepts.md §12's Urgency algorithm needs the whole ancestor chain of Projects (root-first, arbitrary depth) to compute "effective priority," not just a Task's immediate Project. D1.2-2 and 5_GuiClient/Plan.md both just say "Task/Project fields the API already exposes," which reads as if one Task fetch + one Project fetch is enough. It isn't — the GUI needs the whole Project tree (or at least a way to walk parent links), which is fine at Level 1's scale but isn't currently said anywhere.

6. Upstream doc-hygiene, not a flaw in this doc. Level1_Implementation/Scope.md §3 ("core tables carry an OrganisationId... now") directly contradicts DomainModel.md's Tenancy Scope Note ("there is no OrganisationId on these tables because the database itself is the organisation boundary") and the actual schema (no organisation_id column anywhere). The stale wording traces back to Goals.md §4.1, written before §3.2's database-per-tenant decision was settled in the same document. The good news: the real schema and this API plan both correctly follow the later, more specific decision — but Scope.md/Goals.md never got reconciled, so a future reader could be misled.

7. Upstream doc-hygiene. UseCases.md's Open Question #2 ("does the Demonstrator need multi-user concurrent editing") looks already answered by DomainModel.md §4/§5 item 3 ("Level 1 assumes a single user at a time... Decision"). 2_RestApi/Plan.md correctly relies on the settled answer, but UseCases.md still lists it as open.

8. Minor gap in the tracking scheme. "Feature scope" (which use cases are essential for Level 1) is a real, load-bearing open question — both Goals.md §4.1 and UseCases.md's own Open Question #1 raise it, and 2_RestApi/Plan.md §2.1 explicitly depends on it ("if that gets resolved differently, this list should be revisited") — but it's never been given a Q1-N in Level1_Implementation/ImplementationPlan.md, unlike client tech/auth/deployment. There's currently no tracked place for it to actually get resolved.

9. Minor, likely fine but silent. Person deletion (part of UseCases.md's Manage People use case) isn't mentioned as in-scope or explicitly deferred in §2.1/§2.2. Probably correct to omit for Level 1 — DomainModel.md frames soft-disable (is_active) as the primary mechanism — but it's currently just absent rather than a stated decision.

10. Minor. Gating all PersonRole writes to is_organisation_admin (§2.1) means a Team's own SuperUser can't manage their own Team's membership — a reasonable Level 1 simplification, but currently stated as if it's the only possible model rather than a deliberate simplification worth a one-line callout.


─────┬──────────────────────────────────────────────────────────────────────────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  #  │                                           Finding                                            │                                               Status                                                │
├─────┼──────────────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 1   │ Error-mapping / attachment-hash gap                                                          │ Resolved — one service, one request path                                                            │
├─────┼──────────────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 2   │ Missing RLS for Team/Person/PersonRole                                                       │ Resolved — no RLS at all now, ordinary app code                                                     │
├─────┼──────────────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 3   │ Phase 4 needing path-based routing                                                           │ Resolved — back to pure TLS termination                                                             │
├─────┼──────────────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 4   │ Search omits Attachment metadata                                                             │ Still outstanding                                                                                   │
├─────┼──────────────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 5   │ Urgency needs the full Project ancestor chain, not just one Task's Project                   │ Still outstanding                                                                                   │
├─────┼──────────────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 6   │ Scope.md/Goals.md still say "OrganisationId on core tables," contradicting the               │ Still outstanding (upstream, not this doc)                                                          │
│     │ database-per-tenant decision                                                                 │                                                                                                     │
├─────┼──────────────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 7   │ UseCases.md Open Question #2 looks already answered by DomainModel.md                        │ Still outstanding (upstream)                                                                        │
├─────┼──────────────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 8   │ "Feature scope" has no tracked Q1-N, despite this plan depending on it                       │ Still outstanding                                                                                   │
├─────┼──────────────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 9   │ Person deletion not stated as in/out of scope                                                │ Still outstanding (§4.4 now mentions the FK-restrict behavior in passing, but the scope question    │
│     │                                                                                              │ itself is untouched)                                                                                │
├─────┼──────────────────────────────────────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 10  │ PersonRole write gated entirely to org-admin, not flagged as a deliberate simplification     │ Still outstanding                                                                                   │
└─────┴──────────────────────────────────────────────────────────────────────────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────┘

Here are responses to 4-10

4. Include seaching attachments 'meta data'.  The 'meta data' in this case just means the other text fields from 'attachment' that can be searched, i.e., 'name', ' url', ' mail_from'

5. Make the requirement for the whole ancestor chain of Projects to be explicit.  The calculation of Urgency is a key concept and needs to be in Level 1.

6. OrganisationId should not be mentioned as field on the tables in the database, please correct this.

7. Some clarity on this for Level 1.  I do want to have multiple users in Level 1.  However, demonstrations of Level 1 will avoid multiple users editing the same data.  So, multiple users will be able to access and modify data but for Level 1, Conflicts are not a concern as they will be avoided.  For Level 2, Conflict resolution will be required.  Please update the docs to make this clearer.

8. Required for Level 1. 
   Manage Projects/Tasks, Assign resources, Set dependencies, Search, Remarks.
   Gantt/plan view is a Level 1 requirement, since this is key selling point.
   Attachments are required but not for emails.  So URLs and regular files can be attached.
   Admin/support tooling is required.
   Concurrent editing by users of the data is required for Level 1, but not concurrent editing of the same rows of data.  I.e., concurrent editing that causes conflicts can be ignored for level 1.
   
9. People are never deleted.  Make this clear in the documentation.  When people leave, they will be marked as 'is_active' = false.

10. Let's talk about Super User.  Describe the role of Super User, how it is defined/identified and how it is different to is_organisation_admin?



==================

I'm going to ask you to do three things, I think they can be done simultaneously by three agents, but you can decide for yourself.

1. In UseCases.md, the section 'Use Cases Not Carried Forward' should be Section 11.
Move the current 11->12, 12->13, 13->4

2. By the way what do RLS and TLS stand for?  Create a new document called 'Glossary.md' in the 'Claude' directory.  List these in Glossary.md
Glossary.md should appear as a heading for each item, followed by text that describes it.  The headings should be in alphabetical order.
Feel free to add additional items at this time, and at any point later.

3. For each phase's Plan.md document, when the phase is complete, add a section at the end of the document called 'Implementation outcome summary' and include a summary of what was implemented, testing results, any
issues that arose and anything that needs further consideration, either for this phase or for later ones.
Add the above to the Guidelines.

Please recheck the plan.md of 2_RestApi for internal consistency and then across the whole document stack in the scope of Level 1.
Let me know if there are uncertainties or questions that need to be addressed before the implementation and testing of 2_RestApi.

==============

1. Since each 'person' has a 'person_role' which identifies the team, checks must be scoped according to the team.  Each 'project' belongs to a team, and each tasks belongs to a project.  Components do not currently have a 'team_id' they probably should do.  I do definitely want owners of a Remark to be able to edit and delete their own remarks, even read-only users.

2. These need to be migrated to the new terminology.

3. The requirement is correct, the API design needs to be modified to capture this.

4. Q1.3-1 I think is moot.  I need to be able to impersonate any user.  Since each user has their own login username/password, all I need to do is user their username and password.  That is sufficient for Level 1
Q1.3-2 No.  In Level 1 no audit trails of any kind are required.

============

Keep in mind, going forwards, that we don't need any database migration until something is deployed beyond the boundaries of this development environment.  Until then (or if I say different), if DB changes are needed, we can scrub the database and re-create it from scratch with any changes required.  Make a note of this in the ImplementationApproach.md

Go ahead with implementing and testing 2_RestApi.


Will I be able to set up and configure the public/Cloudflare ProjectPal stack on the same machine as I am developing on, in a way that the public/Cloudflare ProjectPal stack is independant from the development environment, e.g., separate DBs, separate servers etc.
I need to be able to develop without disturbing the publich facing ProjectPal.  Periodically, I will want to make new 'release' or 'updates' to the publically facing stack, but this will be very deliberate and not by accident.


I've saved a new screen shot from V1.2 called 'TaskDetail_with_Calendar.png'
In the V1.2 screenshot, notice how the 'Requested Start' and 'End Date' edit boxes have a calendat dropdown icon at the right of the edit box.  When it is clicked, a calendar date select mini-control appears.
This can be seen in 'TaskDetail_with_Calendar.png'.
You can look in the code for V1.2 to see what sort of control this is and how it works.
Can you add the same feature to the new TaskDetail Windows?

I need more data in the database to test with.
Don't make any changes, just tell me what you think of this suggestion.
Can you script the seed data in the database as follows:-
Take all of the data that is currently in the data base and make this all team #1 data.
Look in the SQL Server database ProjectPalDB_1 on this machine and make this all team #2 data - there is a lot of data theMakre.
Put all of this data in some kind of script so that when you rebuild the databases (when I request changes to the schema) 
the script can be used to repopulate the PostgreSql database with this test data. 

1. I understand, yes, there are many rows, and the data needs to be transformed.
2. It if find to change the DBLogin value in the Person table, but I would like to keep that values from the 'Name' column.  I understand the risks, but these are only first names.
3. Attachments don't actually carry any data.  The 'Size' column is populated with a value, but the 'Data' column is actually null.
4. Agreed, this would be a one-off translation.  Going forwards, when the schema changes, the 'scripted' test data would need
to me modified to fit the new schema, there should be no dependency on the SQL Server Database.
Thoughts?

I'd like to add a column to the 'person_role' table called 'nickname', which is a string.
This is so that a real person can have a shorter name that they are known by within a team, e.g., 'Alice' rather than 'Alice Chen'.
The value of 'nickname' may be null.
When populating names on the GUIs in the context of a team, if the 'nickname' column is populated, it should 
be used rather than the 'name' from the 'person' table.
In the test data, populate the nickname for team #1, for Alice, Priya and Nadia to be just theor first names.  Don't populate it for team #2.
No changes yet, just tell me what you think.

How feasible is it to implement the urgency calculation now, using exactly the PP V1.2 algorithm?

When a Team Lead logs in, on the All Tasks view, they should only see tasks for teams that they belong to.





We don't need the 'orig_task_number' column on the task table, or anywhere throughout the code base or windows - please remove it everywhere - regenerate the database.
While regenerating the database, once the data is loaded into the database, create a SQL script that will adjust all of the start_date and due_date fields on the project table.
Where a date is less than 1-Dec-2014, I want to adjust it my adding on 4372 days.
Run this script was it has been created.  
Each time the database is regenerated from now on, going forwards run this script after creating and populating the database, until I say to stop using it.
The idea is that the original data is preserved and this script makes some last minute adjustments that will aid some testing I want to do.
I may change or remove this script in the future.

After doing the above do the following.

Can you double check the urgency calculation detailed in Requirements/KeyConcepts.md against the Project Pal V1.2 code base and verify it is correct and complete in the document.
If you don't need any further clarification from me, can you please go ahead and implement the code needed to support calculating the urgency, then modify the
All Tasks GUI to show the colour coding, using the same colour algorithm as Project Pal V1.2



I need zoom functionality.  
The current display can be taken as the initial definition of 100% zoom for both axes.

By using Ctrl+Mouse scroll wheel I want to be able to zoom in and out horizontally.  The centre date should remain constant by centre I
mean the date of the centre of the area being displayed for the horizontal centre.

By using Shift+Mouse scroll wheel I want to be able to zoom in and out vertically. The centre vertically should remain constant, that is, the central task/project being displayed for the virtical centre.
This means the text size will need to change.
I don't the area used to display the text expand or contract.

If both Shift and Ctrl are pressed, the zoom should be on both axes.

Can you make a little more space under the 'Top Level Projects' title.   I want to add some extra controls in a line beneath the title and above the Gantt graphic area.
The first I want to add is a Horizontal and virtical zoom indicator.  This will be a Label that says 'Zoom: H', followed by an edit box that shows the percentage horizontal zoom, 
followed a label that says 'V', followed by an edit box that shows the percentage vertical zoom.
I'd also like a button that says 'Zoom Reset', that resets both zooms to 100%.
I'd also like a button that says 'Today' which horizontally scrolls the Gantt so that today is positioned 25% from the left of the Gantt graphic area.




That's works and looks good.
I'd like to make a change for zooming in.
Instead of holding the centre of the graphic constant, instead, determine the date and task/project at the current mouse location.
After the zoom in, scroll the window so that the same date and task/project are located at exactly the current mouse location.
This allows the use to focus in on an area of interest.

At both ends of a project box on the Gantt chart, I would like a faint vertical line to be draw downwards to the extent of the its child tasks/projects.

I would like a new line for controls to be added below the current line that contains the zoom indicators.
When the mouse is above a task or project bar on the gantt graphical area, I would like the name of the project/test to be displayed in a read only label control
that is placed on this new line of controls.  The left edge of the label control should be inline with the left edge of the gantt graphic area.
When the mouse move off a task or project bar, the label should be empty.
Look at the code in ProjectPal V1.2 to see how it creates the text from projects and tasks.  It uses a special syntax to show the project hierarchy to the task/project of interest.


As the cursor moves over the I'd like the gantt, I'd the date under the cursor to be shown as text.  The text should be placed at the beginning of the
second line of controls, before the task/project name indicator.  The same style should be used as for the task/project name indicator.
The right hand edge of this control should be 2ems to left of the left hand edge of the gantt graphical area, and the text should be right aligned.

I'd like the gantt graphic area to have a background colour, I'd like to be a very light, pastal green, similar to one used by ProjectPal V1.2, but maybe even slightly lighter than that.

I'd like the Gantt graphical area to have very faint vertical lines across the whole height of the graphic, placed at the beginning of every Monday.\
The colour and thickness of these lines should be the same as the vertical lines at the end of each project bar, however, the opacity should be 50%.
I'd also like similar bars to be should at the start of each day which is the 1st day of month.  The lines should be same as the weekly lines, except they should be twice as thick.



In ProjectPal V1.2, its gantt graphic uses an unusual graphical syntax for projects.  Rather than then being just a bar, they are instead a box, where the box is drawn around all of the child tasks/projects.
I would like to add a tick box
to the top line of controls called 'Boxed', when it is ticked, a box is drawn around all of each projects child tasks/projects.  When not ticked
the current view, behaviour prevails.
In 'Boxed' mode there is a subtle different, the current functionality that shows the project/task name under the mouse (in the second line of controls) should
change so that the project name is shown if the mouse is any where inside a project box.  If there is a child project, then the child's project text is shown rather than 
the parents.
One way to achieve this might be to change the current project 'bar' into a rectangle that is a parent (graphically) for its task/project children.  The contents
of the rentangle possibly needs to made mostly transparant, (e.g. 20% opacity).  This should mean that projects within projects should appear slight darker than their parent.
Have a think, tell me what you think.

---------------------------

Have a go at the following 8 changes.

Take them one at a time, in the order given, they are not dependant upon each other, so no need to consume your whole context with all of them at once.

Do a commit after each one.

At the end, give me a status report for each of them.

If you get stuck on one and need my input, continue with the next one and ask me once there is nothing more you can do.


1. The current 'Dashboard' window is not required.
When I log into Project Pal as a non team-lead, the first window I should see is the All Tasks Window.
A team lead should see the 'MainWindow' when they login - however, that window is not implemented yet, so a team-lead should also see the All Tasks window first until the 'MainWindow' has been developed - make a note.
Implement these changes.

2. The Projects shown on the Project view should only be projects that a user belongs to.

3. There is problem when vertically resizing the window.  The bottom of the Gantt area does not stick to the bottom of the window.  When the vertical size
increase, the space between the bottom of the gantt area and the bottom of the window increases.  When the vertical size gets smaller, the space gets small
and looks like it goes negative, hiding the bottom of the gantt area, that this, the horizontal scroll bar at the bottom become hidded, and an extra vertical 
scroll bar appears on the right for the window as a whole

4. Allow the user to manually expand or contract, horizontally, the area where the Gantt text is displayed.  The Gantt graphical area should should contract/expand at the same time.
On the first line of controls, after the today button, add a check box 'Show Names',  if not ticked, the the Gantt text is not displayed.
If the user completely contracts the area where the Gantt text is displayed (and lets go off the mouse button), then the tick box will automatically switch to off.
If it is not ticked, and then is ticked, the Gantt text is displayed with the default width, (which is the width that is used in the implementation right now).

5. At the bottom, centred underneath each month start marker, I'd like a small text label the indicates the month that is about to begin in the format Mmm.\
Each Jan, the year should be include, e.g., Mmm-YY, e.g., Jan-26.\
This needs to be underneath the gantt graphical area, but above the horizontal scroll bar, so some extra space needs to be added there.

6. Allow the user to re-order the project / task vertically.  The user is not allowed to change the project hierarchy or to change 
the parent project of a task, but they are allow to re-order the projects/task as long as long as it involves no changes
to the database.  They should be able to do this by clicking on a project/task, dragging it up or down and dropping it.
While being dragged, a 50% opaque text label that represents the project/task name should be used.
The drag feature should stop the label when it moves beyond the scope that it may be modified, e.g., a task or project cannot be dragged
beyound the bounds of the parent project.
This change is local to use and can be memorised in a manner that is local to the user (may be with a cookie?), add a memorise order button underneath the text area where the 
gannt project/task names are listed.


7. If I double click on a task/project label on the left hand side text area, the related GUI for the Task or Project should be opened.

8. The tooltip popups on the task and projects on the Gantt graphical area, can you move these about 15 pixel to the right, since when they appear 
the first letter is obscured by the mouse cursor.

Can you add a new tick box to the right of 'Boxed' call 'Weekends'.  If it is not checked, then the graphic area of the gantt chart will exclude weekends.
This means that between each week vertical line, there will only be five days, rather than seven.  If a task/project finishes on a Saturday or Sunday, it will be 
drawn as if it ends at the end of the Friday.  If a task/project starts on a Saturday or Sunday, it will be drawn as if it starts at the beginning of the Monday.
If the check box is checked, then Saturday and Sundays will be included in the display, which is effectively how it is shown right now.  The default value should be not checked.


===========

I'd like you to create a document called 4_GuiClient/PlanViewDesign.md\
In that document I'd like you to document, in detail, all the design decisions and implementation details for the PlanView.  
To be clear, I don't mean to document the code, but I mean to document all of the 'rules' about how it looks and behaves, e.g,
colours, line widths, opaqueness, how the zooms work (what is centred), the drag and drop for task/project titles, the algorithm for formulating
the text in the task/project label at that top left of gantt chart, weekend rules, etc.
Sufficient detail so that someone could code up a PlanView and get a very similar result to the one we have implemented.

I'd like you to create a new directory under V2 called 'UserDocumentation'.
In that directory we will create a document for each of the GUIs.
The documentation in that directory will be aimed at the end user, so it should avoid details about how things are implemented and instead focus on
how things works and how people can use the ProjectPal and the GUIs.
We will, in time, add other documents in there that cover ProjectPal as whole, e.g., IntroductionToProjectPal.md, UserGuide.md etc., which will 
focus on ProjectPal as a whole and common themes.
In each of the GUI documents I'd like you to document each of those GUIs, this can only be done once GUIs are implemented (or ammended)
since they need to document the actual GUIs, not what is planned.
The documents should cover what each GUI shows, its purpose and all the possible ways a user can interact with it, (which may need
to include details on the different types of users and what they are each able to do.)
The GUI documents each need a table of contents.
The GUI documents should be as consistent as possible in terms of layout and major section headings.
We will add more of these GUI focussed documents as we build them - make a note 4_GuiClient/plan.md
We also need a 'Glossary.md' in that directory - again this should be a Glossay of terms that are meaningful to a user of ProjectPal and
should stay away from implementation details unless a user really needs to know.
In addition we need to keep all of the documents in 'UserDocumentation' up to date.
I need you to record that keeping these up to date is an on going and important activity.  
I don't want to update these documents on every turn, but before each major commit
if you feel some of the documents need to be updated, you should let me know or if changes are trivial, do it yourself without asking.
Can you create a new document in Claude/Guidelines called 'UserDocumentationGuidelines.md' that provided full details of all that I have
just described
and ensure it gives sufficient directions to future instanciations of Claude so that they know what they need to do.
Do not create any documents under 'UserDocumentation' in this turn, just update 4_GuiClient/plan.md and create Claude/Guidelines/UserDocumentationGuidelines.md



In the directory V2/UserDocumentation, I'd like you to create four documents, one called 'GanttView.md', one called 'AllTaskView.md'  onee called 'TaskDetailView.md'  and 'Glossary.md'
In each of these I'd like you to follow the UserDocumentationGuidelines.md and create user facing documentation.

==================

On the Gantt GUI, I'd like projects to be collaspable/expandable.  When collasped, all of the children for project are hidden (tasks and projects).
There needs to be a small icon to left of each project title that indicates whether the project is collasped or expanded, pressing the icon will
cause collasped to become expanded or expanded to become collasped.

On the All Tasks window, the tasks are shown in a grid.  The grid is sortable and has a filter.
In addition, we need to add some edit capability to some of the columns, to allow the use to directly edit task from the grid, without having to open
the TaskDetail Window.
Further, other windows will require a task grid with the same features.
This means we need to make common component that can be reused across different windows, lets call this common component the TaskGrid.
The TaskGrid, needs to be configurable for the columns it displays, by default is will show a fixed set of columns, initially, the default
set of columns will be as used in the current All Tasks windows.
The TaskGrid also needs to be configurable as to whether the filter is shown or not.
By configurable, I mean different windows might create an instance of the TaskGrid with different values for the configurable attributes. 
Additionally, each TaskGrid will be given a collection of tasks that it needs to render.
The initial implementation for the TaskGrid can use the grid shown in AllTasks as a template for how it should look and feel.
The task is to:-
1. Document this in UserInterfaceWindows.md
2. Create a common GUI component called TaskGrid that can be reused by any Windows.
3. Make the current AllTasks window use the common TaskGrid, rather than its current grid.  For this to succeed, the look and feel of the TaskGrid in the AllTask window should be identical to the current AllTask window.
In order to test this out, I'd like you to rename the current 'AllTasks' window to 'AllTaskOrig' and create a new implementation for the 'AllTask' windows.
I also want you to make both windows available to me, so that I can compare them side by side.  Once I'm happy the 'AllTaskOrig' window can be deleted from the code base.
No changes yet, give me your thoughts.


1 - I mostly agree, however, across all windows, the experience for any specific user should be the same.  That is, a Team Lead will always see the 'T'.  If
a task editable in a TaskGrid in one window, it will also be editable in a TaskGrid in a different window.  So the Window dictates more about what can see
and settings such as, is the filter shown, but for behaviour, that is not Window specific, but is user specific (e.g., a ReadyOnly user can't edit any tasks
regardless of window, a NormalUser can edit some specific fields of tasks that are assigned to them, but can't edit tasks that they are not assigned to etc.
So the logic about what behavior is permissable should be outside of the TaskGrid, it should be in a central location, but it is not specific to each Window type.

2 - Editable columns for Level 1 should be Status, T, Priority, Owner (Only by the team lead or lead user for tasks within a project they own), Effort, allocation, task type, requestor - whether a column is actually editable depends on the users role and who owns the task or its parent project.

The Save mechanism needs more work.  V1.2 got around this by having all edits held in memory only until the user pressed a 'Save All' button for everything.
With the TaskDetail having its own save button this becomes somewhat inconsistent.
I think this is something we need to worry more about in Level 2, where 'undo' will also be a concern.
So immediate save from grid is OK for Level 1, but will need to be revisted for Level 2.

3 - I agree to your TaskGridPlan.md document.

4 - /tasks should point to the new page.

Thoughts


1. Agreed
2. Agreed, the same permission module should be used throughout, not just for the TaskGrid.
3. Effort Type stays read-only, the use would need to go to the TaskDetail window to change this.  This is to avoid accidental changes, since changing the effort type can have large knock effects.
4. Sounds good.  Once think to check, if a user edits a task field from the TaskGrid, and that task is open in a TaskDetail window, then the TaskDetail window should reflect that change immediately.  Going the other way, any TaskGrids should reflect changes on a TaskDetail window as soons as Save is pressed.  Please confirm.
5. Pefect
Yes, it makes sense to make it visible to the user what they can and cannot edit before they attempt to make an edit.
Tell me your thoughts on the above first.


1.  V1.2 is correct here, the assignee to task should be able to update the detailed description and set the status to InProgress or Ready.  We need this in PP V2.
2.  V1.2 is again correct and we need this in V2
3.  In Level 1 of V2, we need to be able to delete tasks.  The permissions to do this should be the same as V1.2.  Deleting a Task from the TaskGrid should also be possible, but a confirmation diaglog must be used.
4. This is all fine.
5. Good.


=====================

In the Resources list box on TaskDetails for task #10990, Friedel is the assigned resource, but he is not listed in the Resources list box.  I'm guessing that is because he is not listed as a
  rource for that team.  In this case, it is probably the case that he was previously listed as a team resource.
  The list box should contain everyone that is assigned to a task, even if they are not a team resource.  This allows a Lead User to remove that person if they choose to, if they do, then clearly they will
  not be able to add them back as a resource for the Task.  But if they are currently set as a resource for a Task, they should be in the Resource list box.


  ============

