# ProjectPal V2 — GUI / Web Client Phase

## Contents

1. [Status and Purpose](#status-and-purpose)
2. [Scope](#scope)
   - 2.1 [In Scope for Level 1](#in-scope)
   - 2.2 [Deferred](#deferred)
3. [Architecture and Technology](#architecture)
   - 3.1 [Constraints Inherited From Other Phases](#inherited-constraints)
   - 3.2 [Framework and Build Tooling](#framework)
   - 3.3 [Component Library](#component-library)
   - 3.4 [Data Fetching and the Typed API Client](#data-fetching)
   - 3.5 [Routing](#routing)
   - 3.6 [Refresh Strategy](#refresh-strategy)
   - 3.7 [App Shell Presentation (PWA)](#app-shell)
   - 3.8 [Business Logic Placement](#business-logic-placement)
   - 3.9 [Theming and Branding Configurability](#theming)
   - 3.10 [Dropdown / List Ordering](#dropdown-ordering)
   - 3.11 [Screen Layout Principles](#screen-layout-principles)
4. [Multi-Window / Multi-Monitor Interaction Model](#multi-window)
5. [Screen Inventory](#screen-inventory)
6. [Build Order](#build-order)
   - 6.1 [Stage 1 — Foundation](#stage-1)
   - 6.2 [Stage 2 — Pilot: Task List + Task Detail](#stage-2)
   - 6.3 [Stage 3 — Pilot: Gantt / Plan View](#stage-3)
   - 6.4 [Stage 4 — Remaining Screens](#stage-4)
   - 6.5 [Stage 5 — Drag and Drop](#stage-5)
   - 6.6 [Stage 6 — Polishing](#stage-6)
7. [Testing](#testing)
   - 7.1 [Automated Testing](#automated-testing)
   - 7.2 [Manual Testing](#manual-testing)
   - 7.3 [User Documentation](#user-documentation)
8. [Definition of Success](#definition-of-success)
9. [Open Questions (Phase-Specific)](#open-questions)
10. [Decisions (Phase-Specific)](#decisions)

<a id="status-and-purpose"></a>
## 1. Status and Purpose

**Status:** In progress — Stage 1 (Foundation, §6.1) done. Stage 2 (Pilot: Task List + Task Detail, §6.2) done: Task List and a full Task Detail (fields, Project/Component selection, Resources, Dependencies via the "Add Dependency" dialog, Attachments, inline Remarks) all built and verified end-to-end against the real REST API, plus the cross-window drag-and-drop spike (`D1.4-10`) — landed cleanly and built out for real, not deferred. Stage 3 (Pilot: Gantt / Plan View, §6.3)'s core deliverable is built and verified against real seeded data: the bespoke, V1.2-ported hierarchy/positioning/colour-coding/today-marker/dependency-arrow rendering (`D1.4-24`), reachable at `/plan` and `/plan/:projectId`. Explicitly deferred within Stage 3 (§6.3): the resource-loading swimlane, the People/Status/Priority colour-mode toggle, and the secondary per-Resource workload-over-time chart. Stage 4 (Remaining Screens, §6.4) has begun: Project Detail is built and verified end-to-end against the real REST API, per `ProjectDetailPlan.md`'s own design/plan — reachable at `/projects` (Top Level Projects) and `/projects/:projectId`, including the sub-Project/Task tree, Create/Rename/Delete/Add Task dialogs, and the Dependencies/Attachments/Remarks tabs (`DependenciesPanel` generalised to a Task-or-Project owner along the way, `D1.4-46`). Component List/Component Detail is now also built and verified end-to-end against the real REST API, per `ComponentDetailPlan.md`'s own design/plan (`D1.4-66`/`D1.4-70`–`D1.4-72`) — reachable at `/components` (Top Level Components) and `/components/:componentId`. Search/Find is now also built and verified end-to-end against the real REST API/database, per `SearchPlan.md`'s own design/plan (`D1.4-73`–`D1.4-82`) — reachable at `/search`, including the `components/DenseDataGrid.tsx` chrome extraction shared with `TaskGrid.tsx` and `search.py`'s own multi-word AND-matching fix. Manage People and Team Management (the latter pulled forward from an original Level 2 deferral, `D1.4-85`) have a confirmed combined design (`ManagePeoplePlan.md`, `D1.4-83`–`D1.4-87`) but are not yet built — this is the one piece of Stage 4 work that also needs real server-side changes (§6 there), not GUI-only. The remaining Stage 4 items (Admin tooling, the MainWindow landing dashboard) are not yet designed or built.

Build a browser-based web app as the client GUI for the Demonstrator, per `D1-1` in `../ImplementationPlan.md`. This is the largest phase in Level 1 by feature surface: it's responsible, end to end, for every screen a user actually touches, plus two pieces of derived logic that live entirely on the client — the Gantt/plan view's layout and the Urgency calculation.

<a id="scope"></a>
## 2. Scope

<a id="in-scope"></a>
### 2.1 In Scope for Level 1

Settled by `D1-4` (`../ImplementationPlan.md`) and `../Scope.md`: Manage Projects/Tasks, Assign Resources, Set Dependencies, Search, Remarks, the Gantt/plan view, and File/Link attachments are all required — not optional trial content. Admin/support tooling is required. Concurrent multi-user access is required; conflict handling for two users editing the same record at once is not (`D-DM-3`).

`Requirements/UseCases.md`'s View the Plan (Gantt) use case is required for Level 1 (`D1-4` — a key selling point, not a deferral candidate) and is this phase's responsibility end to end — the database and API need nothing special for it (`../2_RestApi/Plan.md` §2.2). The plan/Gantt view is built by composing the Task, Project, and Dependency data the REST API already exposes (schedule derivation, layout, and rendering all happen client-side), not by a bespoke aggregation endpoint.

Urgency (`Requirements/KeyConcepts.md` §12) is also this phase's responsibility end to end, per `D1.2-2` in `../2_RestApi/Plan.md` — computed client-side from the Task/Project fields the API already exposes, not served pre-computed. Being dynamic (it changes with the passage of time alone, with no underlying data change) fits naturally with computing it where it's displayed rather than re-fetching it. Team-specific configurable weights for the algorithm are a likely later refinement (not Level 1 — see `Claude/Level2_Implementation/Scope.md`), and fit this GUI-side placement more naturally than a server-side per-Team lookup would.

**Urgency needs the whole Project ancestor chain, not just one Task's Project.** `Requirements/KeyConcepts.md` §12's "effective priority" factor is computed root-first over *every* ancestor Project above a Task (the Task's own Project, that Project's parent, and so on), not just the immediate one — a Project tree can be arbitrarily deep via `parent_project_id`. This phase needs to fetch the whole Project tree (or otherwise be able to walk parent links for any Task's ancestry) to compute Urgency correctly — a single Task fetch plus its one immediate Project is not sufficient. Level 1's data volumes (one Organisation, a handful of Teams/Projects) make fetching the whole tree trivial; this stops being free once Level 2/3 have many tenants/Projects, but that's out of scope here.

`5_UrgencyCalculation/` was expected to fold entirely into this phase — a claim this document now corrects: it's tracked as its own phase instead, now that it's actually starting (`5_UrgencyCalculation/Plan.md`), since Urgency turned out self-contained enough to plan and land on its own rather than as a sub-thread of this much larger phase.

**Urgency is now available — use it wherever a Task is shown, colour-coded where appropriate.** `5_UrgencyCalculation/Plan.md` provides `lib/schedule.ts`'s `computeUrgency` (the score, `Requirements/KeyConcepts.md` §12.1) and `computeUrgencyColour` (the highlight colour, §12.2), both already wired into All Tasks and Task Detail (its own `D1.5-5`). Any remaining screen this phase builds that shows a Task — the Gantt/plan view (§6.3) most directly, since colour-coding rows by urgency is explicitly one of its own use cases, but equally Project/Component Detail's own Task lists in Stage 4 — should include Urgency and colour-code it wherever doing so is actually appropriate for that screen, rather than treating it as an All-Tasks-specific feature. Nothing needs building here to get this — both functions already exist once `5_UrgencyCalculation` lands; this is a note for whoever builds those screens to remember to actually call them.

<a id="deferred"></a>
### 2.2 Deferred

- Merge/conflict-resolution UI (`Requirements/UserInterfaceWindows.md` §3.18) — no Level 1 GUI screen needed, per `D-DM-3`.
- The old app's Private-Task visibility toggle and its `ConfigWindow` (§3.15) — dropped from the domain model; nothing to build.
- A dedicated impersonation UI — not needed per `D1.3-1`; an admin can just log in as the target Person directly.
- Real-time push updates (WebSockets/live collaboration) — see §3.6 below.
- Drag-and-drop as the primary interaction for reparenting and dependency creation — not ruled out forever, but not Level 1's starting design; see §5 and §6.2 below for how the initial build stays ready to add it without rework.
- A native/installable client alongside the web app — `Claude/Level2_Implementation/Scope.md`.

<a id="architecture"></a>
## 3. Architecture and Technology

<a id="inherited-constraints"></a>
### 3.1 Constraints Inherited From Other Phases

Three decisions made in earlier phases shape this one before any GUI-specific choice gets made:

- **The build output must be static files.** `6_HttpsReverseProxy/Plan.md`'s `D1.6-4` already commits Caddy to path-based routing where `/api/*` goes to the REST API and `/` serves this phase's static files directly — there is no second "GUI server" process in the stack, and adding one would mean revisiting a decision already made and documented. This rules out any framework whose default deployment model is a persistent Node.js server (e.g. Next.js in SSR mode) unless used purely in a static-export mode. A conventional single-page-application (SPA) build — HTML/CSS/JS bundled by a build tool, served as-is — fits what's already been decided without any further discussion.
- **The API base URL must be configurable, not hardcoded, and a cross-origin dev request needs a real answer before Phase 6 exists.** A browser calling `http://localhost:8000` directly from the Vite dev server's own origin (`http://localhost:5173`) is genuinely cross-origin — it fails on CORS unless the API grants it, which would mean adding (and later removing) API-side CORS configuration purely for a dev convenience. Resolved by `D1.4-11`: the GUI's API base URL is the env-configurable `VITE_API_BASE_URL` (`gui-client/.env.example`), set to the relative `/api`, and Vite's dev server proxies `/api/*` to the REST API (`gui-client/vite.config.ts`) — the same relative path and prefix-stripping behaviour Caddy's `/api/*` routing (`D1.6-4`) will use in production, so this needs no later change at all, and the browser never makes a cross-origin request in the first place.
- **Auth is a bearer JWT, not a cookie/session.** `3_Authentication/Plan.md` already settled the token shape and `POST /auth/login` contract. The GUI's job is: collect credentials, call that endpoint, hold the returned JWT (in memory plus `sessionStorage` so a page refresh doesn't force a re-login mid-session), and attach it as `Authorization: Bearer <token>` on every subsequent API call. No cookie handling, no CSRF concerns, no server-side session store.

<a id="framework"></a>
### 3.2 Framework and Build Tooling

**Decision (see `D1.4-1` below): React, TypeScript, and Vite.**

Reasoning:
- Vite's default build output for a React app *is* a static bundle — it matches §3.1's constraint with no extra configuration or "static export mode" opt-in to get wrong.
- React is the most widely used UI framework by a wide margin, which matters concretely for a project built with heavy AI-assistant involvement: more training data and community precedent means fewer novel mistakes, and a wider pool of maintainers later if this ever needs a second pair of hands.
- TypeScript pairs naturally with the REST API, which already publishes a real OpenAPI document (`../2_RestApi/Plan.md`). Generating types (and a client — §3.4) directly from that document keeps the GUI's understanding of the API's shape mechanically in sync with the API itself, rather than hand-copied and prone to drifting silently out of date.
- This is a conventional, low-risk choice, not an exotic one — there was no strong reason found to prefer Vue, Svelte, or Angular instead; any of them would have satisfied §3.1 equally well, but React's ecosystem depth (component libraries, Gantt-adjacent libraries evaluated in Stage 3, community answers to obscure problems) is the deciding factor given a solo-developer-plus-AI-assistant team shape.

<a id="component-library"></a>
### 3.3 Component Library

**Decision (`D1.4-1`, bundled with §3.2): MUI (Material UI), specifically its core component set plus the `@mui/x-data-grid` and `@mui/x-tree-view` extensions.**

ProjectPal's UI is fundamentally an enterprise data-management tool — grids, trees, detail forms, modal dialogs — not a marketing site or a consumer app, and MUI is built squarely for that shape of application:
- `@mui/x-data-grid`'s free/Community tier (sortable, filterable, paginated columns) directly covers what `TaskWindow`/`ProjectWindow`-style list grids (`Requirements/UserInterfaceWindows.md` §3.2, §3.4) need for Level 1 — nothing here requires the paid Enterprise tier (row grouping, pivoting, etc. aren't Level 1 requirements).
- `@mui/x-tree-view` directly covers the Project/Component hierarchy trees (§3.5, §3.8) without a bespoke tree component being built from scratch.
- MUI is (like React itself) the most widely used React component library, for the same AI-assistant-leverage reason given in §3.2.

Ant Design was considered as a credible alternative (equally comprehensive, equally aimed at admin-tool-style UIs) but wasn't chosen over MUI absent a specific reason to prefer it; either would have worked.

<a id="data-fetching"></a>
### 3.4 Data Fetching and the Typed API Client

**Decision (`D1.4-1`, bundled): TanStack Query for server-state (caching, loading/error states, refetch-after-mutation), fed by a client typed directly from `../2_RestApi/`'s published `openapi.json` (via `openapi-typescript` for types, `openapi-fetch` as the thin typed fetch wrapper).**

Almost everything this GUI holds in memory *is* server state (a Task, a page of search results, the current user's identity) rather than genuine client-only state (which form field is focused, whether a dialog is open). TanStack Query is purpose-built for the former and removes most of the hand-rolled loading-flag/error-flag/stale-cache bookkeeping a simpler `fetch`-in-`useEffect` approach would otherwise need throughout every screen. A heavier general-purpose state library (Redux, MobX) wasn't chosen because there's very little of the *latter* kind of state that would justify it — local component state (`useState`) is enough for what's left.

Generating types from `openapi.json` rather than hand-writing them means a change to the API's contract shows up as a TypeScript compile error in the GUI immediately, rather than as a runtime failure discovered later.

<a id="routing"></a>
### 3.5 Routing

**Decision (`D1.4-1`, bundled): React Router, with a real, stable, deep-linkable URL for every item** — `/tasks/:taskId`, `/projects/:projectId`, `/components/:componentId`, and so on, plus list routes (`/tasks`, `/projects`) and the standalone `/plan` Gantt view. This isn't just a routing-library choice — clean per-item URLs are the load-bearing mechanism behind §4's multi-window decision below, so this is worth calling out as a first-class requirement on every detail screen from Stage 1 onward, not an afterthought.

<a id="refresh-strategy"></a>
### 3.6 Refresh Strategy

**Decision (`D1.4-1`, bundled): no real-time push (no WebSockets, no polling timer).** Data refreshes when a screen is navigated to or a browser tab regains focus (TanStack Query's default `refetchOnWindowFocus` behaviour), and after any mutation the GUI itself makes. `Requirements/DomainModel.md`'s Cross-Cutting Concerns already accepts that Level 1 avoids concurrent-edit conflicts through light real usage rather than building conflict-resolution machinery (`D-DM-3`); a live-push mechanism would be solving a problem (staleness during someone else's edit) that Level 1 has already decided not to treat as a real risk. `Requirements/KeyConcepts.md`'s Merge/Conflict entry already flags real-time collaboration as a Level 2 question, not a Level 1 one — this keeps that boundary intact rather than quietly building half of it now.

<a id="app-shell"></a>
### 3.7 App Shell Presentation (PWA)

**Decision (`D1.4-5` below): package the GUI as an installable Progressive Web App** — a `manifest.json` (`display: "standalone"`, name, icons) plus a minimal service worker, on top of the same React/Vite build already decided (§3.2). No new language, runtime, or packaging pipeline.

An ordinary browser tab carries an address bar, other tabs, and a bookmarks bar — it doesn't read as "an app." A `standalone`-display PWA strips all of that: once installed (via the browser's own "Install app" prompt), it opens in its own chrome-less window, gets its own icon in the Start Menu/taskbar, and appears as its own entry in Alt-Tab, distinct from the browser. This is the same underlying browser windowing behaviour as launching Chromium with `--app=<url>`, but delivered through a W3C-standard mechanism the browser already provides, rather than a custom shortcut/launcher that would need building and distributing separately.

A native wrapper (Electron, Tauri, or a genuine native client in C#/Java) was also considered and set aside for Level 1: a native client is an explicitly *deferred*, not-yet-committed Level 2 option (`Claude/Level2_Implementation/Scope.md`), and building one now would mean walking back `D1-1` for no functional gain the PWA route doesn't already deliver. Tauri specifically is worth keeping in mind as the natural upgrade path if a genuinely native capability is wanted later (a system tray icon, offline use, OS-level drag-and-drop between windows) — it wraps the same web codebase built here in a thin native shell, rather than requiring a rewrite the way a C#/Java client would.

A PWA install prompt needs a secure context (HTTPS, or `localhost`, which browsers already treat as secure for this purpose). Development against `localhost` during Stages 0–3 is unaffected; full installability for real users naturally lands once `6_HttpsReverseProxy` exists, which the build order already has coming after this phase.

<a id="business-logic-placement"></a>
### 3.8 Business Logic Placement

**Decision (`D1.4-6` below): authoritative business rules live server-side, in Python, in the REST API; the GUI is deliberately kept thin.**

The dividing line: anything that must hold true regardless of which client is asking — validation, authorization (Team-scoped checks already enforced in `rest-api/app/security/deps.py` per `D-UC-4`), anything protecting data integrity — belongs in the API, because the API is the actual trust boundary. A browser client today, and any future client, both have to go through it, and only server-side enforcement can be relied on to actually hold.

The **only** logic that legitimately lives client-side, in TypeScript, is the narrow exception already decided elsewhere in this plan: Urgency (`D1.2-2`) and Gantt schedule derivation (§2.1, §6.3) — both pure, presentation-time computations over data the client already legitimately holds, recomputed because they're cheap and dynamic rather than because the GUI is where business rules generally belong. Any future candidate for client-side logic should be tested against the same question before being added, rather than treating this carve-out as a general licence.

Within that carve-out, schedule-derivation math (given Tasks/Dependencies/Resources, compute each Task's effective position on the timeline) is written as plain, dependency-free TypeScript functions, kept separate from the React components that render the Gantt bars — unit-testable on its own, and, usefully, portable to any other JS/TS runtime later (a Tauri shell, a future non-browser client) with no rework, since it has no DOM dependency.

**Confirmed concretely, not just anticipated (`D1.4-14` below).** Neither `V1.2` nor `V2` ever stored a Task's Start/End dates — both store only a business-day offset from the owning Project's start date (`8_ValidationAndVerification/Plan.md` §4.1). `V1.2`'s GUI computed `EarliestStartDate`/`StartDate`/`Duration`/`EndDate` for display every time (`V1.2/Libs/DBProjectPal/DBProjectPal/Task.cs`), rather than the database ever holding them — exactly this carve-out, already in production in the app this is replacing. `gui-client/src/lib/schedule.ts` reproduces that computation (Stage 2's bounded version — one level of predecessor-Dependency awareness, not the full recursive graph walk Stage 3's Gantt view needs, which will extend this same module rather than duplicate it). The general principle this confirms: **before assuming a displayed value must come from an API field, check whether the value was ever actually stored anywhere, in either version** — `V1.2`'s own source is as authoritative a check as the database schema is, and is available to read (`Claude/Level1_Implementation/4_GuiClient/Plan.md` `D1.4-13`'s layout-reference principle applies here too, just for computed *values* rather than layout).

<a id="theming"></a>
### 3.9 Theming and Branding Configurability

**Decision (`D1.4-12` below): every visual choice — colours, font, logo/icon paths, app name — lives in one file, `gui-client/branding.json`, not scattered through components.**

The visual design (fonts, colours, logos, icons) isn't settled yet and is expected to change once there's something real to react to and give feedback on (`Requirements/Goals.md`'s "let real users try the concepts and the GUI and give feedback" framing applies just as much to how it looks as to what it does). Rather than hunt through every screen's `sx` props when that feedback arrives, every value that would change in a rebrand is pulled from `branding.json`, and everything else reads from it rather than holding its own copy:

- `src/theme/theme.ts` builds the MUI `Theme` (`palette.primary`/`secondary`/`background`, `typography.fontFamily`) from `branding.json`, and every component uses MUI's theme (`ThemeProvider`, `sx` tokens like `primary.main`) rather than a hardcoded colour value.
- `src/theme/Logo.tsx` is the one place the app name and logo image are rendered, reused by the login screen and the app shell — a rebrand changes `branding.json`, not every screen that happens to show the logo.
- `vite.config.ts`'s PWA manifest (§3.7) reads the same file for its `name`/`short_name`/`theme_color`/`background_color`/icon, so the installed-app identity matches the in-app one automatically.
- `index.html`'s favicon, `theme-color` meta tag, and `<title>` are placeholders overwritten from `branding.json` by a small `transformIndexHtml` plugin (`vite.config.ts`'s `brandIndexHtml`) — the one place that couldn't just `import` the JSON directly, since it's static HTML, but still ends up sourced from the same file rather than holding its own hardcoded values.
- The font (Inter) is self-hosted via `@fontsource/inter` rather than linked from Google Fonts' CDN — no external network dependency at runtime, which matters for a Demonstrator that may run inside a customer's own network (`Scope.md` §1) with no assumption of outbound internet access for the GUI itself.

This is deliberately **build-time** configurability, not a runtime theme editor: changing `branding.json` and rebuilding is the whole workflow. A live, in-app theme-switcher (an admin settings screen, persisted per-Organisation choice, hot-reloaded styling) is real additional engineering — a settings UI, a place to store the choice, a way to apply it without a rebuild — that a single-organisation Level 1 Demonstrator doesn't need; worth reconsidering if multi-tenant branding becomes a genuine Level 2/3 requirement (`Claude/Level2_Implementation/Scope.md`), since at that point different Organisations plausibly *do* need different branding simultaneously from the same running deployment, which this build-time approach can't do.

<a id="dropdown-ordering"></a>
### 3.10 Dropdown / List Ordering

**Decision (`D1.4-15` below): dropdown option order is a deliberate UX choice inherited from V1.2, not incidental — match it, not the database enum's declaration order.**

`V1.2/Apps/ProjectPal/ProjectPal/Tasks/GUITaskColumns.cs`'s `GetComboValues_static` populates every dropdown (Priority, Status, Task Type) in a specific, clearly-chosen order (most-urgent-first for Priority; a specific non-alphabetical order for Status and Task Type) — not the order the values happen to be declared in any schema. `V2`'s enum declaration order in `database/migrations/001_initial_schema.sql` is arbitrary by comparison and was, before this was caught, exactly what `gui-client/src/api/types.ts`'s `PRIORITY_LEVELS`/`TASK_STATUSES`/`TASK_TYPES` constants used. Fixed to match V1.2's order name-for-name for Status and Task Type (identical value sets in both versions); Priority required inference, since V1.2's five active levels (`VHigh`/`High`/`Med`/`Low`/`VLow`) don't name-match V2's five (`High`/`MedHigh`/`Med`/`MedLow`/`Low`) — ordered most-urgent-first by position, with the exact historical correspondence flagged for confirmation (`8_ValidationAndVerification/Plan.md` `Q1.8-4`) rather than asserted as certain. Person-list dropdowns (Owner/Requestor/Resources) already matched — both versions use insertion/`person_id` order — but V1.2 additionally restricts these three to *active* People only (`Person.AllActiveInstances`), which V2's had not; matched now too.

<a id="screen-layout-principles"></a>
### 3.11 Screen Layout Principles

**Prompted by comparing real screenshots side by side** — `Claude/Requirements/V1.2_ScreenShots/TaskDetail_1.png` against `Claude/Requirements/V2_ScreenShots/TaskDetail_260901.png` (both folders grow over time as more screens are built and compared; see `Requirements/UserInterfaceWindows.md` §1). The comparison found V2's Task Detail using roughly 4-5x the screen area of V1.2's for the same information, working against the multi-window goal `D1.4-8` already committed to: a screen a user can only fit one of at a time, at a large size, defeats the point of being able to see several at once. V1.2's own window was **534×673px** — smaller than a single one of the compact mockups built to explore fixing this — and still fit everything V2 has today plus Links and a full Dependency panel besides.

Three mockup directions were drafted and published for review (not yet decided which, if any, to build — see `Q1.4-17`) — a Claude Design canvas comparing all three against the actual V1.2 screenshot. Studying both screenshots and building those mockups surfaced the following reusable techniques, worth applying to every future detail screen (Project Detail, Component Detail — Stage 4), not just Task Detail:

- **Size fields to their content, not to an equal grid column.** V1.2 gives Priority/Status a narrow field and Description a wide one; V2's current 4-column CSS grid gives every field the same width regardless of what it holds. A `flex-wrap` layout with explicit per-field widths (or `min-content`/`max-content` sizing) replaces the equal-fraction grid.
- **Collapse identity into one compact header, not a full field.** V1.2 shows the Task's own description as large, unlabelled text directly under a slim one-line strip (Task ID/OrigId/Owner/Urgency) — not literally in the OS window's title bar (that stays generic, "ProjectPal: TaskDetail" — a claim from earlier discussion this document now corrects), but visually functioning as one, and taking a fraction of the vertical space a bordered, labelled `TextField` needs. The mockups reproduce the visual effect (a slim identity bar) and go further where the web platform actually offers a real title bar equivalent: setting `document.title` to the item's own description dynamically, so a multi-window user can tell open windows apart from the OS taskbar/Alt-Tab without V1.2 ever needing to solve that (it only ever has one window per object open at a time; V2's model, §4, can have several).
- **Share one tabbed area between low-frequency sub-panels, with a count in each label.** V1.2's Remarks/Attachments/Links share one tab strip — only one is ever visible, and each tab's label carries a count ("Remarks (0)") so a user can tell at a glance whether there's anything there without switching to it. V2 currently gives Resources/Dependencies/Attachments/Remarks four permanently-expanded cards, all visible at once, which is most of the extra vertical space.
- **Bound long pick-lists to a small scrolling area instead of letting them grow the page.** V1.2's Resources checklist is a small fixed-size listbox; V2's wraps across the full page width and grows with the Team's People count. Three refinements make this concrete rather than just "make it a scrollable box":
  - **Checked-first two-phase sort, recomputed on every toggle.** Split the list into checked and unchecked, sort each group alphabetically, then concatenate checked-first. This is derived from current state on every render (not a one-off sort applied at load), so ticking or unticking an entry immediately moves it to its new position. The practical effect: whatever is actually assigned is always visible at the top, above the fold, even if the full list needs scrolling to see everything else — a user checking "who's on this Task" never has to scroll to find out.
  - **Truncate displayed names to a fixed maximum length** (10 characters, `D1.4-18` — a starting value, easy to revisit), not the full name, so one long name can't blow out the column width; show the full name as a tooltip/`title` on hover so nothing is actually lost, just deferred.
  - **Fixed width and height, not "grows to fit."** With the name length capped and a capped visible row count (5 rows before scrolling), both dimensions of the listbox are now known constants rather than content-dependent — treat it as a fixed-size box in layout, the same way an icon or an avatar is, not as a text container that reflows around its content.
- **Don't let a fixed-size element leave empty space beside it — pack a neighbour into the same row.** Once the Resources listbox above has a fixed, small footprint, giving it a full-width card of its own (as the first compact-mockup pass did) leaves most of that card empty. V1.2 never does this: its Resources listbox sits in the left column with the Requested/Planned/End Date fields and the Affected Component/Projects fields packed into the same visual row to its right — nothing is given more horizontal space than its content needs, and neighbours fill what a fixed-size control doesn't use. Applied to V2: pair the Resources listbox with the Detailed Description field in one row within a single card, rather than stacking Resources and Description in separate full-width cards.
- **Use dense control sizing throughout.** V1.2's controls are sized in the 20-30px range; MUI's `size="small"` variant (roughly 32-34px) is the closest equivalent V2 has available and is markedly more compact than the default (~56px) size used everywhere today.
- **Progressive disclosure for the less-frequently-needed fields.** V1.2's Dependency panel is collapsed by default (a `+`/`-` toggle) unless a Task actually has dependencies to show. One mockup direction generalises this into per-section accordions (Scheduling, Resources, Dependencies, Attachments, Remarks all independently collapsible) — the most aggressive of the three on default compactness, at the cost of an extra click to see something collapsed.

None of this is decided yet — see `Q1.4-17` below.

<a id="multi-window"></a>
## 4. Multi-Window / Multi-Monitor Interaction Model

This resolves `Requirements/UserInterfaceWindows.md`'s `Q-Win-3`.

**The old app's behaviour.** V1.2 is a WinForms desktop app where every detail screen is a genuine, independent, freely-positionable OS-level window, with a singleton-per-object pattern (double-clicking the same Task twice re-focuses its existing window rather than opening a duplicate). On a large screen or a multi-monitor setup, a user can have the Task list on one monitor, a specific Task's detail open on another, and the Gantt view on a third, all visible and interactive simultaneously. Several documented interactions (e.g. dragging a Task from one open window onto a Project in another) directly depend on this.

**Options considered for a browser-based web app:**

- **A — App-managed pop-out windows.** Use `window.open()` to spawn genuine separate browser windows per item, each independently draggable to any monitor, with the app tracking which windows are open (to implement singleton-per-object re-focusing) and synchronising state between them (e.g. via `BroadcastChannel`, so editing a Task in one window tells a sibling window showing the same Task to refetch). This most faithfully reproduces V1.2's behaviour, but is real, ongoing engineering: window-registry bookkeeping, cross-window messaging, and popup-blocker edge cases (browsers block `window.open()` calls not triggered directly by a user click/gesture — manageable, since double-clicking a row qualifies, but a real constraint to design around).
- **B — Rely on the browser's own native window handling for placement, but use named window targets for singleton-per-object.** If every item has a real, stable URL (§3.5) and every "open this item" action calls `window.open(url, name)` with a **deterministic name per item** (e.g. `task-123`, not `_blank`), the browser itself refuses to open a duplicate: a second call with the same name navigates and refocuses the *existing* window instead of opening a new one. This is native, standard `window.open` behaviour, not something built by hand. Combined with each such window being an installed PWA window (§3.7 — chrome-less, independently positionable on any monitor), this reproduces V1.2's actual pattern — many *different* items open across a user's monitors, never two windows for the *same* item — without a custom window registry, `BroadcastChannel` messaging, or the popup-blocker concerns Option A's more general approach raises (a `window.open` call triggered directly by a click, which double-clicking a row or clicking "open in new window" both are, isn't blocked).
- **C — An in-app docking/tiling workspace** (VS-Code-style split panes within a single browser window, via a library such as `react-mosaic` or `golden-layout`). Gives a multi-pane experience inside one window, but not genuine separate OS windows spanning multiple physical monitors unless combined with Option A or B anyway — meaning it adds real complexity (a docking-layout library, its own state to persist) without actually answering the multi-*monitor* part of the question on its own.

**Decision (`D1.4-8` below, superseding `D1.4-2`): Option B.** The default interaction — clicking a Task row from a list, following a cross-reference — navigates the *current* window in place; this is the common case, and popping a new window for every click is exactly the "explosion of duplicate windows" the old app's singleton pattern existed to prevent. A separate, explicit "open in new window" action (plus Ctrl/middle-click on links, per normal browser convention) is what calls `window.open(url, name)` with the item's deterministic name — giving both a direct equivalent to V1.2's double-click-to-pop-out habit *and* true singleton-per-object re-focusing, from the start, using a browser primitive rather than hand-rolled bookkeeping. Cross-window data staleness (editing a Task in its own window doesn't instantly update a list showing it in another) is unaffected by this decision and remains covered by the refetch-on-focus behaviour already decided in §3.6.

Cross-window *drag-and-drop* (dragging an item from one open window onto another — the specific old-app example this question was raised over) is **not** delivered by this decision on its own; see §6.2's drag-and-drop-readiness principle and `D1.4-10` below — its spike landed cleanly and was built out for Level 1 (Ctrl-drag a Task between two open Task Detail windows to create a Dependency), not deferred.

<a id="screen-inventory"></a>
## 5. Screen Inventory

**Layout reference principle (`D1.4-13` below): V1.2's actual screen layouts are a strong hint for each V2 screen, not a constraint.** `V1.2/Apps/ProjectPal/ProjectPal/` has the real WinForms `.Designer.cs` (and a few WPF `.xaml`) files for every window — exact field grouping, order, and emphasis, refined by real usage over time, and worth taking seriously as a tried-and-tested starting point. But V1.2 is WinForms-era technology from some years ago; where a modern web/MUI pattern serves the same goal better, that wins over faithfully reproducing the old arrangement. Building each screen (§6) starts with reading the corresponding `.Designer.cs`/`.xaml` file(s) for how V1.2 actually laid it out, then adapting deliberately — not ignoring it, and not copying it uncritically either. This is the same judgment call already made for specific interactions elsewhere in this plan (`D1.4-4`'s explicit pickers over drag-and-drop, §5's inline Remarks below), generalised here to layout as a whole.

Maps every window in `Requirements/UserInterfaceWindows.md` §3 to its V2 equivalent, and flags where V2 deliberately modernises rather than replicates the old interaction:

| V1.2 Window (§) | V2 Equivalent | Level 1? | Notes |
|---|---|---|---|
| MainWindow (3.1) | App shell + dashboard | Yes | Simplified nav hub; the per-Resource workload report becomes a dashboard panel, not the sole landing page |
| TaskWindow (3.2) | Task List | Yes | `@mui/x-data-grid`; its embedded Gantt tab becomes a link to the standalone `/plan` view rather than a second Gantt renderer |
| TaskDetail (3.3) | Task Detail | Yes | Stage 2 pilot — richest single screen; see §6.2 |
| ProjectWindow (3.4) | Project List | Yes | |
| ProjectDetail (3.5) | Project Detail | Yes | Hierarchical tree via `@mui/x-tree-view`; built in Stage 4 once the pattern is proven |
| NewProject (3.6) | Create/rename Project dialog | Yes | Simple modal |
| Plan Display (3.7) | Gantt / Plan View | Yes (`D1-4`) | Stage 3 pilot — built, `/plan` + `/plan/:projectId`, see §6.3 |
| ComponentWindow (3.8) | Component List/Detail | Yes | Reuses Project Detail's tree pattern |
| NewComponent (3.9) | Create/rename Component dialog | Yes | Simple modal |
| RemarkWindow (3.10) | Inline Remarks panel | Yes | Modernised: no separate window — an inline comment thread embedded directly in Task/Project/Component Detail, built once as part of the Stage 2 pilot and reused everywhere |
| Find (3.11) | Search | Yes | |
| Manage People (3.12) | Manage People | Yes | Admin-only |
| — (no V1.2 equivalent) | Team Management | Yes (`D1.4-85`) | New in V2 — a Team-Lead-facing (or admin, for any Team) screen managing one Team's own membership/role/`is_resource`/nickname; pulled forward from an original Level 2 deferral (`D-DM-11`) once real trial-team usage showed it's needed from the start |
| New Person (3.13) | Create Person dialog | Yes | Simple modal |
| AdminWindow (3.14) | Admin tooling screen | Yes (`D1-4`) | Thin UI over the already-built `/admin/export` and `/admin/integrity-check` endpoints — trigger buttons plus a results panel, not a rebuild of the old window |
| ConfigWindow (3.15) | — | No | Private-item visibility dropped from the domain model; "hide closed Projects" folds into an ordinary list filter |
| PasswordWindow (3.16) | Login screen | Yes | Real password login (`3_Authentication`) replaces whatever the old app did here |
| Progress Indicator (3.17) | Loading spinners | Yes | An ordinary UI pattern (MUI's own components), not a distinct screen to design |
| Merge Dialogs (3.18) | — | No | Deferred, `D-DM-3` |
| GridFilterSelect (3.19) | Grid column filter | Yes | `@mui/x-data-grid`'s built-in column filtering — not a bespoke component |

Two interactions `Requirements/UseCases.md` explicitly flags as "UX choices to redesign for the new client technology" rather than commitments to preserve get a Level 1 answer here, both favouring explicit controls over drag-and-drop for the first build:

- **Assign Resources to a Task** (`UseCases.md` #2) — kept as an explicit multi-select checklist in Task Detail, matching the old app's actual model; the use case's own open question ("rather than drag-and-drop directly onto the Gantt view") is answered here: not for Level 1. Drag-onto-Gantt is a plausible future enhancement once the Gantt view itself (Stage 3) exists to drop things onto.
- **Set Dependencies** (`UseCases.md` #4) — the old app's drag-between-two-listboxes interaction becomes an explicit "Add Dependency" search-and-pick dialog (search or browse for a Task/Project, add it as a predecessor/successor). Simpler to build correctly than a drag target, and no less usable for Level 1's trial-scale data volumes.

Both are revisitable UX refinements, not architectural commitments. What actually determines whether drag-and-drop can be added later without rework isn't the presence of a DnD library now — it's whether the underlying data mutations (reparent a Task, add a Dependency, assign a Resource) are written as standalone, reusable functions rather than inlined into a dialog's save handler; see `D1.4-7` and §6.2.

<a id="build-order"></a>
## 6. Build Order

Sequenced in stages rather than a flat list, so that the two riskiest technology/architecture questions get answered by building real, feature-rich screens early — per the instruction that shaped this plan, the first screen(s) built should carry enough feature weight to actually exercise the stack, not be a trivial "hello world."

<a id="stage-1"></a>
### 6.1 Stage 1 — Foundation

Not itself a proving-ground screen, but the scaffolding every later stage depends on: project setup (Vite + React + TypeScript), the generated OpenAPI client (§3.4), the login screen and JWT handling (§3.1), the app shell/navigation frame and routing skeleton (§3.5), and the dashboard/workload-report panel (§5's MainWindow equivalent — a simpler build than the pilots, doesn't need its own proving-ground treatment).

<a id="stage-2"></a>
### 6.2 Stage 2 — Pilot: Task List + Task Detail

The first architecture-proving pilot. Task Detail (`UserInterfaceWindows.md` §3.3, "the most heavily used window in the app") is deliberately chosen for its breadth: it exercises permission-gated field rendering (Team-scoped roles from the JWT), a Project/Component picker, the dependency panel (§5's redesigned "Add Dependency" dialog), the Resource-assignment checklist, the inline Remarks panel, and the Attachments panel (upload, list, download/open for File and Link kinds) — nearly every reusable sub-component the rest of the GUI will need gets built once, here, then reused unchanged by Project Detail and Component Detail in Stage 4. A minimal Task List (grid, no advanced filtering yet) is built alongside it purely as the navigation entry point into Task Detail, not as a pilot in its own right — `@mui/x-data-grid` handles most of its complexity already.

By the end of this stage, the framework/library choices in §3 are validated against real, complex, stateful UI, not just a form or two.

**Drag-and-drop readiness (`D1.4-7`).** None of Stage 2's data-changing interactions (reparenting via the Project/Component picker, adding a Dependency, assigning a Resource) are built as logic inlined into a dialog's save button. Each is a standalone, reusable mutation (e.g. a `useReparentTask()` hook wrapping the underlying API call) that the explicit picker/dialog UI calls today, and that a drag-drop handler could call unchanged later — adding drag-and-drop becomes wiring a new interaction on top of an existing mutation, not reworking the mutation itself. This costs nothing extra now (it's already good practice for testability) and is the actual prerequisite for keeping drag-and-drop viable, not any particular library choice. In-window drag-and-drop (e.g. within the Gantt view, or a list next to a tree in the same screen) is a straightforward later addition on this foundation using a library such as `@dnd-kit`.

**Cross-window drag-and-drop spike (`D1.4-10`).** Before the end of this stage, spend a short, scoped spike on the one interaction that most exercises it. Revised spike shape (was: dragging a Task onto a Project to reparent it) — chosen to mirror V1.2's own Ctrl-drag-creates-a-Dependency pattern (`Requirements/UserInterfaceWindows.md` §4's "Ctrl-modified drag effect" cross-cutting note) rather than the Move-effect reparenting interaction:

- **Source:** Ctrl+drag the Task's own title *within* a Task Detail window — the task's displayed title text in the page body, not the browser/OS window's title bar.
- **Target:** drop onto a *different*, already-open Task Detail window (a different Task than the one being dragged).
- **Landing zone:** the drop must land specifically in that target window's **Dependencies** tab/panel, onto either the **"Depends upon"** or **"Dependants"** list — matching V1.2's own two-listbox target (`UserInterfaceWindows.md` §3.3's Drag and Drop Behaviour entry) — not just anywhere on the window.
- **Auto-switch behaviour:** if the target Task Detail window's Dependencies tab is not the currently active tab when the drag enters that window, activate it automatically (so the two drop targets are visible and reachable without the user needing to have pre-selected that tab).
- **Resulting mutation:** creates a Dependency between the two Tasks (direction determined by which list — "Depends upon" vs "Dependants" — it's dropped onto), wired to the same `useCreateDependency()` mutation the explicit "Add Dependency" dialog (`D1.4-7`, §5) already calls — the spike is about the drag-and-drop *mechanics* across two windows, not a new mutation.

This still exercises the hard, previously-unresolved question — native HTML5 drag-and-drop doesn't reliably cross separate top-level browser windows, so this needs hands-on investigation, not a guess from documentation — while testing it against the specific interaction the app will actually need (Dependency creation), rather than the reparenting interaction the original spike description used.

**Outcome: the spike landed cleanly and was built out for real** (not deferred). Confirmed by hand across two separate `openItemWindow`-created Task Detail windows: native HTML5 drag-and-drop (`dragstart`/`dragover`/`drop`) fires correctly across them — the standalone, `window.open()`-created windows (not tabs) that were the actual unverified assumption behave the same as ordinary tabs for this purpose. What was built:
- **Drag source:** the small icon badge next to the Task's title in `TaskDetailPage.tsx` (the title itself is a live-editable `<input>` bound to `description`, not a plain label, so it can't double as the drag source without colliding with native text-selection/editing drag — the badge is the adapted equivalent). `onDragStart` cancels the drag entirely (`preventDefault`) unless Ctrl is held, matching V1.2's own convention.
- **Auto-switch:** a page-level `onDragOver` on the target window checks `dataTransfer.types` for a custom MIME type (`application/x-projectpal-task`, `lib/dnd.ts`) and switches the local `subTab` state to Dependencies (index `0`) if it isn't already active — deliberately doesn't revert on drag-leave.
- **Drop targets:** the "Depends upon" and "Dependants" `List`s in `DependenciesPanel.tsx` each get `onDragOver`/`onDragLeave` (hover highlight) and `onDrop` (reads the dragged Task's id, guards against dropping onto its own window, then calls the *same* `useCreateDependency()` mutation the existing "Add Dependency" dialog already uses — no new mutation code, per `D1.4-7`'s whole point). Creation is immediate on drop, same as the dialog — not staged behind the page's own Save button, which only governs the Task's own editable fields.
- Verified: both directions (Depends upon / Dependants), a plain drag with no Ctrl held does nothing (cancelled at the source), and dropping a Task onto its own window does nothing (silently guarded, mirroring V1.2's `if (preTask == postTask) return;`).

Not automatable through this project's usual headless-CDP verification flow — dispatching a real cross-window OS-level drag gesture isn't something CDP does reliably — so this was verified by hand instead.

<a id="stage-3"></a>
### 6.3 Stage 3 — Pilot: Gantt / Plan View

The second architecture-proving pilot, kept deliberately separate from Stage 2 because it's a genuinely different kind of technical problem — layout/scheduling computation and custom rendering, not forms and grids — and `UseCases.md` itself flags it as needing its own evaluation as "one of the most technically demanding [screens] to reproduce outside WinForms."

This stage was originally planned as a hands-on spike comparing two MIT-licensed React Gantt libraries (`gantt-task-react`, `frappe-gantt`) before committing to one. Revised (`D1.4-24`) before that spike ran: build bespoke instead, porting V1.2's own from-scratch Gantt implementation (`V1.2/Libs/PlanDisplay/`) rather than adopting a third-party library at all.

**Why not a library.** Checking the two originally-listed candidates directly against npm rather than relying on memory: `gantt-task-react` hasn't been published since 2022-07-10 and its peer dependency is pinned to `react: ^18.0.0` — this project is on React 19, so it's not viable. `frappe-gantt` is still real and actively maintained (6.1k GitHub stars, published as recently as 2026-02-25) with built-in dependency arrows and drag-to-reschedule, but it's vanilla JS/DOM (would need a React wrapper) and has no documented hierarchical/grouped-row support — a real gap, since V1.2's actual Gantt tree is a genuine Project→sub-Project hierarchy (see the Component-vs-Project correction below). A newer, React 19-compatible alternative (`react-modern-gantt`, MIT, native hierarchy support) exists, but is small and unproven (43 GitHub stars, 1 open issue). More decisively: **neither library, nor any other evaluated, supports V1.2's resource-loading swimlane** (`Libs/PlanDisplay/ResourceLevel.cs`'s per-day effort-accumulation area chart under the bars) — any library choice would need that bolted on regardless, which weakens the case for reaching for one at all. Commercial options (Bryntum, DHTMLX's paid tier) and DHTMLX's free GPL edition remain set aside for the same reasons as before (no meaningful free tier; GPL-licensing risk for a closed-source product) and weren't reconsidered.

**Why bespoke, and why it's low-risk here.** V1.2's Gantt (`Libs/PlanDisplay/Task.cs`, `Project.cs`, `PlanControl.xaml.cs`, `TimeBox.cs`, `Constants.cs`, `ResourceLevel.cs`, `ResourceLevelTask.cs` — 3,131 lines total) is itself a from-scratch implementation with no third-party dependency, built directly on WPF's `Canvas`. Its actual algorithmic content — `Constants.cs`'s `PixelsPerDay`/`TaskHeight` day-to-pixel positioning model, `ResourceLevel.cs`'s per-day effort-accumulation and swimlane-height stacking — is portable, framework-independent logic, not WPF-specific boilerplate, and a small proven reference to port rather than a design problem to solve from scratch. This also matches this project's own established preference (`D1.4-6`): presentation/business logic lives in owned TypeScript, not a third-party package, for anything domain-specific — the same reasoning that put Urgency and schedule derivation in `lib/schedule.ts`.

**Scope correction (also `D1.4-24`, and `Requirements/UserInterfaceWindows.md` §3.7).** Re-checking V1.2's actual tree-building code (`Apps/ProjectPal/ProjectPal/GanttDisplayHelper.cs`) directly found that §3.7's documented "Task/Project/Component bars recursively laid out from a Project" was wrong: there are two entirely separate tree-builders in that file, one walking `Project → SubProjects` (+ each Project's own `Tasks`) — used by the standalone Plan Display and `TaskWindow`'s embedded Gantt tab — and one walking `Component → SubComponents` (+ each Component's own `Tasks`) — used only by `ComponentWindow`'s own separate embedded Gantt tab. Components never appear inside the Project-scoped Plan Display. Fixed in `UserInterfaceWindows.md` §3.7/§3.8. Stage 3's pilot is scoped to the Project-hierarchy view only; the Component-scoped variant is a straightforward reuse of the same rendering code once Component Detail exists (Stage 4), not part of this pilot.

**What this stage builds:** `lib/ganttLayout.ts` (pure TypeScript, no DOM — day-to-pixel positioning ported from `Constants.cs`, and a function turning Projects/Tasks/Dependencies plus a `buildScheduleGraph` result into plain layout data: row positions for the Project→sub-Project→Task tree, each bar's `{x, width, y}`, dependency-arrow endpoints — unit-tested the same way `lib/schedule.ts` already is), and `features/plan/PlanPage.tsx` (an SVG renderer consuming that layout data: bars coloured via the already-built `computeTaskRowColour`, dependency arrows, a "today" marker, double-click-to-open-Task via `openItemWindow`), reachable at its own route(s) (`/plan`, `/plan/:projectId`), opened as its own singleton window per `D1.4-8` rather than in-place navigation.

**What this stage explicitly defers**, each a legitimate separable follow-up rather than a corner cut: the resource-loading swimlane (`ResourceLevel.cs`'s algorithm, a second rendering problem from the bars themselves — worth its own pass once the core view lands, deferred to Stage 6, `D1.4-40`); V1.2's People/Status/Priority colour-mode toggle (V2 ships Urgency-colouring only for now, consistent with what's already built elsewhere — deferred to Stage 6, `D1.4-40`); the Component-scoped Gantt variant (stays assigned to Stage 4, unlike the other three items here — it's a straightforward reuse of the already-built rendering code once Component Detail exists, not a genuinely risky or hard-to-judge-in-advance piece of work); and the secondary per-Resource workload-over-time chart (a standard bar/area chart, e.g. via `recharts` — genuinely a different, lower-risk problem from the Gantt bars, doesn't need this stage's own evaluation, but deferred to Stage 6 anyway, `D1.4-40`, alongside its two siblings above).

<a id="stage-4"></a>
### 6.4 Stage 4 — Remaining Screens

Once both pilots have validated the stack, roll out the rest of §5's inventory, reusing what Stage 2/3 already built:

- Project List + Project Detail — **Project Detail is done** (the hierarchical tree pattern, `@mui/x-tree-view`; dependency/attachment/remarks panels reused from Stage 2, generalised to a Task-or-Project owner along the way, `D1.4-46`), per `ProjectDetailPlan.md`, reachable at `/projects` and `/projects/:projectId`. **Project List itself is not built** — no separate flat/sortable grid exists yet (`ProjectDetailPlan.md` §1's own scoping); `/projects` with no id is Project Detail's own "Top Level Projects" mode instead, matching the Gantt view's own `/plan` duality.
- Component List + Component Detail (reuses Project Detail's tree pattern).
- Search/Find.
- Manage People (admin grid + create-Person dialog) — needed before real trial-user onboarding can begin, though not blocking for initial internal testing against the already-seeded People.
- Admin tooling screen (thin UI over `/admin/export` and `/admin/integrity-check`).
- A real MainWindow-equivalent landing dashboard (§5's MainWindow row) — in scope for Level 1 (`D1.4-39`), replacing the placeholder stub `D1.4-25` removed. Distinct from the richer per-Resource workload-over-time chart (deferred to Stage 6, above) — this is a simpler landing summary, its actual content to be designed when this stage reaches it. Once built, `D1.4-25`'s own follow-up applies: route a `TeamLeadUser` there, every other role continues to land on `/tasks`.

**Drag and drop deferred to Stage 5.** Per `D1.4-40`, each window built in this stage gets the same explicit-control treatment Stage 2 already established for Task Detail (`D1.4-4`'s tree pickers, `D1.4-7`'s "Add Dependency" dialog) rather than V1.2's own drag-and-drop interactions (reparenting a Project/Component by dragging its title, dragging a Task onto a Project/Component row, OS-file drag onto an Attachments grid, Ctrl-drag onto a Dependencies list from a second window) — those are this stage's own drag-and-drop surface, picked up in Stage 5 once these windows actually exist to drag between.

This stage is deliberately left at survey level here — it's built from patterns the pilots will have already settled, so detailed screen-by-screen design is deferred until Stage 4 actually starts, consistent with how other not-yet-reached phases in this plan are documented. `ProjectDetailPlan.md` and `TaskGridPlan.md` (both this folder) are exceptions to that — detailed designs and implementation plans written ahead of this stage actually starting, at the user's own request. `TaskGridPlan.md` (`D1.4-48`–`D1.4-50`) also reaches back into already-shipped Stage 2 work: it factors All Tasks' own grid into a reusable `TaskGrid` component (for this and future Task-list-showing screens) and adds inline cell editing, with a small, deliberate consistency fix to Task Detail's own Owner field along the way.

<a id="stage-5"></a>
### 6.5 Stage 5 — Drag and Drop

Collects every drag-and-drop interaction deferred from Stage 4 (`D1.4-40`) — reparenting a Project or Component by dragging it onto a new parent, dragging a Task onto a Project/Component row to move it there, OS-file drag-and-drop onto an Attachments grid, and cross-window Ctrl-drag Dependency creation from a Project's or Component's own Dependencies tab (the same mechanism Stage 2's `D1.4-10` spike already proved out for two Task Detail windows, extended to the window types Stage 4 adds).

This is its own stage, after Stage 4 rather than folded into it, specifically because it needs those windows to already exist: `D1.4-10`'s own spike depended on two real, already-open Task Detail windows to drag between, and the same is true here — a Project Detail ↔ Project Detail drag, or a Task ↔ Project Detail drag, can't be built or tested meaningfully before both window types are real. Each Stage 4 window's own drag-and-drop is deferred here individually, not as a single lump — some may be pulled forward into Stage 4 itself later, if it turns out straightforward to build alongside a given window rather than waiting.

<a id="stage-6"></a>
### 6.6 Stage 6 — Polishing

Collects work explicitly deferred out of earlier stages because it's either lower-risk than what it was deferred from, or genuinely easier to get right once more of the rest of the system already exists around it (`D1.4-40`). Currently:

- The resource-loading swimlane (`ResourceLevel.cs`'s per-day effort-accumulation area chart under the Gantt bars) — deferred from Stage 3.
- V1.2's People/Status/Priority colour-mode toggle for the Gantt view — deferred from Stage 3.
- The secondary per-Resource workload-over-time chart — deferred from Stage 3.
- Switching the Gantt view's Project-visibility check (`lib/ganttLayout.ts`'s `isVisibleProjectPriority`) over to the full `isProjectActive` (`lib/schedule.ts`) Project Detail introduces (`D1.4-41`) — deferred from Stage 4, since it's a real behaviour change to already-shipped, tested code, kept separate from Project Detail's own unrelated build.
- Project Detail's own sub-Project/Task tree (`ProjectDetailPlan.md` §4.4): a per-row task-count/priority summary badge, and a per-row "Gantt Display" shortcut button (`D1.4-42`) — deferred from Stage 4, since it's hard to judge exactly how much per-row detail is worth showing until the tree exists and gets real use.
- `TaskGrid`'s own governed cells (`TaskGridPlan.md` §4.5) are only distinguished from non-editable ones by a subtle background-colour difference (`READONLY_BG`) — the user reported it isn't clear at a glance which fields can actually be edited and which can't, and asked for this to be addressed as Stage 6 polishing specifically, not now — deferred from Stage 4.

More items are expected to accumulate here as Stage 4/5 work turns up further things worth deferring for the same two reasons — risk reduction (building the more architecturally demanding or foundational pieces first) and better-informed design (some things are hard to get right until more of the surrounding system is actually in place to judge them against). This stage is deliberately left at survey level, the same as Stage 4, until it actually starts.

<a id="testing"></a>
## 7. Testing

Manual testing against the running `rest-api` (real seeded People, real Argon2id login) is the primary coverage for this phase, rather than a mocked API layer — consistent with how `2_RestApi`/`3_Authentication` were tested, and avoiding a second, parallel "does the mock match the real API" concern. No end-to-end/browser-automation suite is being built for Level 1 — deferred to Level 2 (`D1.4-9`).

<a id="automated-testing"></a>
### 7.1 Automated Testing

None yet for this phase's own screens. Unit tests for the Urgency calculation itself are `5_UrgencyCalculation/Plan.md`'s responsibility (§6.1 there — that phase is introducing this project's first test runner, Vitest, rather than waiting for Stage 3 as this line previously said). Gantt schedule-derivation math (§3.8, §6.3) is still planned for Stage 3, when that logic first exists, kept as plain functions separate from rendering specifically so it's testable the same way.

<a id="manual-testing"></a>
### 7.2 Manual Testing

**Kept up to date as each Build Order stage (§6) lands** — this section describes what can actually be checked in the app *today*; each stage adds to it rather than replacing it, the same way `3_Authentication/Plan.md` §6.3 built on `2_RestApi/Plan.md` §6.5 instead of repeating it. Currently covers Stage 1 (Foundation) and the Task List/Task Detail portion of Stage 2.

**1. Start both halves of the stack.** The GUI dev server isn't part of `docker-compose.yml` (§3.1) — start the API/database and the GUI separately. From `V2/`:
```powershell
docker compose up -d
```
Then, from `V2/gui-client/`:
```powershell
npm run dev
```
Open <http://localhost:5173>. The dev server proxies `/api/*` to the REST API on `:8000` (`D1.4-11`) — if the dashboard shows "Could not reach the API," check `docker compose ps` first.

**2. Log in.** Use any seeded Person's credentials (`V2/README.md` §9, or `3_Authentication/Plan.md` §4, for the full table) — e.g. `alice.chen@example.com` / `alice-pass1`. Confirm a successful login redirects from `/login` to `/`, and that a wrong password (or unknown login) shows the "Login failed" message rather than a stack trace or a blank page.

**3. Confirm role-based UI.** Alice and Nadia are `is_organisation_admin` — log in as one of them and confirm the "Admin" chip appears in the app bar; log in as a non-admin (e.g. Ben) and confirm it doesn't.

**4. Confirm the dashboard reflects real data.** The "Teams" count card should match the actual number of Teams in the seeded database — cross-check via `GET /team` in the REST API's Swagger UI (`2_RestApi/Plan.md` §6.5) if in doubt.

**5. Confirm route guarding and logout.** While logged out (or in a private/incognito window), navigating directly to `http://localhost:5173/` should redirect to `/login`, not show the dashboard. After logging in, **Log out** should clear the session and return to `/login`; refreshing afterward should not silently log you back in.

**6. Confirm the PWA installs as a standalone app (`D1.4-5`).** Open `http://localhost:5173` in a real (non-headless) Chrome or Edge window and use the browser's "Install app" prompt (or the install icon in the address bar). Confirm it opens in its own chrome-less window — no address bar, no tabs — with the ProjectPal icon and name (`branding.json`, §3.9).

**7. Open the Task List.** Click **Tasks** in the app bar (`/tasks`). Confirm the grid shows real seeded Tasks, with Project/Owner names resolved (not raw IDs) — cross-check a row against `GET /task` in the REST API's Swagger UI if in doubt. Double-clicking a row navigates to `/tasks/<id>`.

**8. Confirm Task Detail loads and edits correctly.** Open any seeded Task (e.g. `/tasks/1`). Confirm every field is populated from real data (Description, Project, Component, Priority, Status, Task Type, Owner, Requestor, Effort/Person Days-Duration, Detailed Description). Change a field (e.g. Description) and click **Save** — confirm the change persists after the page reloads, and that the Task List reflects it too.

**9. Confirm Resources, Dependencies, Attachments, and Remarks all round-trip against the real API.** On a Task's detail page:
   - **Resources** — toggling a Person who *is* a resource on that Task's Team should succeed; toggling one who isn't should show a dismissible error ("Couldn't assign … — they may not be a resource on this Task's Team") rather than failing silently or reverting with no explanation. The checklist deliberately shows every Person, not a pre-filtered eligible set (see §6.2 for why) — this is expected, not a bug to report.
   - **Dependencies** — **Add Dependency** opens a dialog to search for another Task and pick "Depends upon" or "Is depended upon by"; the new dependency appears in the correct list immediately. The trash icon removes one.
   - **Attachments** — **Add Link** (name + URL) and **Add File** (a real file picker) both add an entry that appears immediately; clicking a File attachment downloads it, clicking a Link attachment opens it in a new tab.
   - **Remarks** — adding one appears immediately with the author's real name and timestamp.

**10. Confirm the "open in new window" affordance (`D1.4-8`).** In All Tasks, double-click a row. Confirm it opens a Task Detail window for that task; double-clicking the *same* row again re-focuses that existing window rather than opening a second one (the named-`window.open` mechanism §4 describes). (Task Detail itself no longer has its own "open in new window" icon on its header — see `D1.4-8`'s note below on why that specific instance was removed.)

**11. Confirm the computed date fields (`D1.4-14`).** On a Task with an assigned Project (with its own `start_date`) and a non-zero start offset, confirm Requested Start Date, Planned Start Date, and End Date all show plausible computed dates (not blank, not "Invalid Date"), and that End Date is later than Planned Start Date by roughly `effort_in_days` ÷ (assigned Resource count) calendar days for a Person Days Task. Changing the Effort or the start offset and re-checking (before Save) should update these immediately — they're computed from the live form, not just the last-saved values.

**12. Confirm dropdown order matches V1.2 (`D1.4-15`).** Open the Priority, Status, and Task Type dropdowns on any Task and confirm the option order is: Priority — High, MedHigh, Med, MedLow, Low, Cancelled, Closed; Status — Closed, Cancelled, InProgress, NotStarted, Ready, Support, Tentative; Task Type — Enhancement, Maintenance, NewDevelopment, Other, Support, Infrastructure. Confirm Owner/Requestor/Resources only list active People (no inactive/deactivated seeded Person appears).

**13. Confirm cross-window drag-and-drop Dependency creation (`D1.4-10`).** Open two different Tasks in two separate Task Detail windows (double-click two different rows in All Tasks). In the target window, switch away from the Dependencies tab. Ctrl-drag the source window's title icon badge onto the target window: confirm its Dependencies tab activates automatically, confirm a dashed highlight appears while hovering over either the "Depends upon" or "Dependants" list, and confirm dropping onto one adds the dragged Task there immediately (no Save needed) — repeat for the other list to confirm the reverse direction. Confirm a plain drag with no Ctrl held does nothing, and dropping a Task onto its own window does nothing.

**14. Confirm the Gantt / Plan View (`D1.4-24`).** From the app bar's "Plan" button (opens its own singleton window, `D1.4-8`), confirm a "Top Level Projects" view renders: Project rows (flat blue-grey bars) each followed by their own Tasks (Urgency-coloured bars, same colouring as All Tasks/Task Detail), a dashed red "today" line, and arrows between Tasks/Projects that have a Dependency between them. Double-click a Task bar and confirm it opens that Task's own Task Detail window (`openItemWindow`, same mechanism as All Tasks' row double-click). Navigate to `/plan/<a real top-level project id>` directly and confirm the view scopes to just that Project's own subtree (fewer bars, heading shows the Project's name instead of "Top Level Projects"). A Closed/Cancelled Task, or a Cancelled/Closed-priority Project, should not appear at all.

**15. Confirm Project Detail (`ProjectDetailPlan.md`).** From the app bar's "Projects" button, confirm a "Top Level Projects" view renders — every top-level Project you can see, each with an expand arrow. Click into a real seeded Project (e.g. `/projects/1`): confirm its Priority/Start Date/Due Date/computed End Date/Parent Project/Detailed Description all show real data, and (if you're the owner or a Team Lead User on its Team) that editing a field enables Save and persists after reload. Confirm the sub-Project/Task tree lists that Project's own children (Tasks first by computed start date, then sub-Projects alphabetically), each Task showing its own Urgency colour; toggle the **Tasks** filter (None/Open/All) and confirm the tree's own Task rows change accordingly. If you're a Lead User or Team Lead User on the Project's Team: confirm **Add New Project** creates a real sub-Project (appears in the tree immediately), its own pencil icon renames it, and its own trash icon (and the header's own **Delete** button) deletes it after confirming — a Delete blocked by something still referencing the Project should show a clear message, not a raw error. Confirm **Add Task** requires Description/Component/Task Type before it will create anything, and that a successful create opens the new Task's own Task Detail window. Confirm the Dependencies tab's "Add Dependency" dialog can find and link both Tasks and Projects, and that Attachments/Remarks round-trip the same way they already do on Task Detail.

**Not yet testable:** the resource-loading swimlane and colour-mode toggle (deferred within Stage 3, §6.3), Project List (no separate grid built yet — `/projects` with no id is Project Detail's own top-level mode instead), and the remaining Stage 4 screens aren't built yet.

<a id="user-documentation"></a>
### 7.3 User Documentation

Every screen this phase builds also needs its own end-user-facing document under `V2/UserDocumentation/`, once that screen is real (or, once its document exists, updated when the screen is amended) — see `Claude/Guidelines/UserDocumentationGuidelines.md` for the full rules on what goes in that directory, how each GUI document is structured, and the ongoing obligation (checked before each major commit, per that document's §6) to keep them in sync with the actual, current behaviour of the app. This note exists so that obligation stays visible from this phase's own tracking document, not only from the Guidelines document itself — `AllTasks.md`, `TaskDetail.md`, `PlanView.md`, and one per further screen this phase adds, all belong there as each lands.

<a id="definition-of-success"></a>
## 8. Definition of Success

- Every item in `D1-4`'s required feature list has a working screen, reachable from the app shell, backed by the real REST API.
- The Gantt/plan view renders real seeded data correctly, including dependency lines and Urgency-driven colour-coding.
- A user can open two or more items (e.g. a Task and its Project) in separate windows, positioned independently on separate monitors, and interact with both; opening the same item a second time re-focuses its existing window rather than creating a duplicate.
- The app installs as a PWA (via the browser's "Install app" prompt against `localhost` during development) and opens in a chrome-less, app-styled window, not a browser tab.
- The GUI's build output is a static bundle with no server process of its own, ready for `6_HttpsReverseProxy` to serve as-is.

<a id="open-questions"></a>
## 9. Open Questions (Phase-Specific)

- **`Q1.4-63`** — Sizing inside `ProjectDetailPage.tsx`'s own tree area, now that an expanded `Project` can contain a full, `autoHeight` `TaskGrid`: does the tree's current fixed `maxHeight: 320` scroll box still make sense, or does it need to grow/change alongside this work? Deliberately kept open: build the initial implementation with this box unchanged, and revisit once there's a real embedded-`TaskGrid` tree to judge it against. Full detail in `ProjectsGUIComponent.md` §7.

<a id="decisions"></a>
## 10. Decisions (Phase-Specific)

- **D1.4-1** (decided 2026-08-30)<br>
  **Question:** Framework, component library, data-fetching, and routing technology for the GUI (§3).<br>
  **Decision:** React + TypeScript + Vite (static SPA build, matching `D1.6-4`'s Caddy-serves-static-files routing); MUI (`@mui/x-data-grid`, `@mui/x-tree-view`) as the component library; TanStack Query for server-state, fed by a client generated from `../2_RestApi/`'s `openapi.json` (`openapi-typescript` + `openapi-fetch`); React Router with a real per-item URL for every entity; no real-time push (WebSockets/polling) — data refreshes on navigation, tab focus, and after mutations. See §3 for full reasoning per sub-choice.
- **D1.4-2** (decided 2026-08-30)<br>
  **Question:** How should V2's browser-based GUI support the multi-window/multi-monitor workflow V1.2 offered natively as a desktop app (`Requirements/UserInterfaceWindows.md`'s `Q-Win-3`)?<br>
  **Decision:** rely on the browser's own native tab/window handling (any tab can be dragged to its own window on any monitor; Ctrl/middle-click opens a new tab) rather than building app-managed pop-out windows with a window registry and cross-window sync. The only engineering requirement this places on the GUI is that every item has a real, stable, deep-linkable URL (§3.5) — plus one small addition, an explicit "open in new tab" action on every detail screen, giving a direct equivalent to V1.2's double-click-to-pop-out muscle memory. Automatic singleton-per-object re-focusing (V1.2's behaviour when the same item is opened twice) is knowingly not reproduced; each tab independently re-fetches current data (§3.6), so this is a minor convenience gap, not a correctness one.<br>
  **Superseded by:** `D1.4-8`
- **D1.4-3** (decided 2026-08-30)<br>
  **Question:** Which GUI screen(s) should be built first to prove the technology/architecture choices, and in what order should the rest follow (§6)?<br>
  **Decision:** two pilots, not one, reflecting that the Gantt view is a genuinely different technical problem from everything else: Stage 2 builds Task List + Task Detail (chosen for breadth — it exercises the dependency panel, resource assignment, remarks, and attachments sub-components in one screen, all of which get reused unchanged later); Stage 3 builds the Gantt/plan view, starting with a hands-on spike comparing `gantt-task-react` and `frappe-gantt` against real data before committing to one. Only once both pilots validate the stack does Stage 4 roll out Project/Component List+Detail, Search, Manage People, and Admin tooling, reusing the patterns the pilots establish. Stage 1 is foundational scaffolding (login, routing, API client) ahead of both pilots, not itself a proving-ground stage.
- **D1.4-4** (decided 2026-08-30)<br>
  **Question:** Should Level 1 preserve the old app's drag-and-drop interactions for Assign Resources (`UseCases.md` #2) and Set Dependencies (`UseCases.md` #4), both explicitly flagged there as "UX choices to redesign"?<br>
  **Decision:** no, not for the initial build — both become explicit controls instead (a multi-select checklist for resource assignment; a search-and-pick "Add Dependency" dialog for dependencies), each simpler to build correctly than a drag target and no less usable at Level 1's trial data volumes. Revisitable later as a UX refinement; not an architectural commitment either way — see `D1.4-7` for how the initial build stays ready for it.
- **D1.4-5** (decided 2026-08-30)<br>
  **Question:** How should the GUI look and feel like a real app rather than a browser page, given ordinary browser chrome (address bar, tabs, bookmarks) undermines that — and should this be a browser-based PWA, a `chromium --app` launcher, or a genuine native client (C#/Java)?<br>
  **Decision:** package the GUI as an installable Progressive Web App (`manifest.json` with `display: "standalone"`, plus a minimal service worker) on top of the same React/Vite build (§3.2) — no new language or runtime. This delivers the same chrome-less, independently-positionable app window as `chromium --app`, through a standard, browser-native install mechanism rather than a custom launcher/shortcut to build and distribute. A native client (C#/Java) was rejected for Level 1: it's an explicitly deferred Level 2 option (`Claude/Level2_Implementation/Scope.md`), would mean walking back `D1-1`, and — per `D1.4-6` — would duplicate business logic in a second language for no functional gain the PWA route doesn't already deliver. See §3.7.
- **D1.4-6** (decided 2026-08-30)<br>
  **Question:** Where should the business logic this GUI needs actually live, and in what language, given it needs to be maintained over time?<br>
  **Decision:** authoritative business rules (validation, authorization, anything protecting data integrity) live server-side, in Python, in the REST API — the API is the real trust boundary, and only server-side enforcement can be relied on to hold regardless of which client is asking. The GUI itself is kept thin; the only client-side (TypeScript) logic is the narrow, already-decided exception of Urgency (`D1.2-2`) and Gantt schedule derivation (§6.3), both pure presentation-time computations over data the client already holds. Schedule-derivation math is written as plain, DOM-independent TypeScript functions separate from rendering, for testability and portability to any future JS/TS runtime. See §3.8.
- **D1.4-7** (decided 2026-08-30)<br>
  **Question:** Given drag-and-drop is dropped from the initial build (`D1.4-4`) but considered important, what should be built now so adding it later doesn't require rework?<br>
  **Decision:** every data-changing interaction (reparenting, adding a Dependency, assigning a Resource) is implemented as a standalone, reusable mutation function/hook, called by the initial explicit-picker UI and, unchanged, by a future drag-drop handler — the mutation and the interaction that triggers it are kept decoupled from the start. This is the actual prerequisite for drag-and-drop readiness, not any DnD library choice, and costs nothing extra now. In-window drag-and-drop is a straightforward later addition on this foundation (e.g. via `@dnd-kit`); cross-window drag-and-drop is materially harder (native HTML5 drag-and-drop doesn't reliably cross top-level browser windows) and is resolved separately by `D1.4-10`'s spike, not assumed solved by this decision. See §6.2.
- **D1.4-8** (decided 2026-08-30)<br>
  **Question:** Revisiting `D1.4-2`: should V2's multi-window model also reproduce V1.2's singleton-per-object re-focusing (never two windows for the same item), rather than accepting independent-tabs-with-possible-duplicates as sufficient?<br>
  **Decision:** yes — every item gets a real, stable, deep-linkable URL (§3.5); ordinary navigation (clicking a row, following a cross-reference) navigates the current window in place; an explicit "open in new window" action (plus Ctrl/middle-click on links) calls `window.open(url, name)` with a deterministic name per item (e.g. `task-123`), which the browser itself uses to refocus an already-open window for that item rather than opening a duplicate — true singleton-per-object behaviour, delivered by a standard browser primitive rather than a hand-built window registry or `BroadcastChannel` sync. Combined with `D1.4-5`'s PWA packaging, each such window is chrome-less and freely positionable on any monitor, matching V1.2's actual multi-monitor usage. Cross-window data staleness remains covered by §3.6's refetch-on-focus behaviour; cross-window drag-and-drop is explicitly out of this decision's scope (`D1.4-10`). Full reasoning: §4.<br>
  **Correction (2026-09-06):** Task Detail's own header briefly had an "open in new window" icon calling this same mechanism *on itself* — removed as a genuine no-op, not merely redundant: since it only ever rendered inside a task's own already-open window, and a named `window.open()` call resolves against the *current* browsing context first if its name already matches, clicking it could only ever re-focus the very window it was clicked in (which the OS had already focused for the click itself). The affordance is only meaningful invoked *from a different context* than the target's own window — TaskListPage's row double-click is exactly that, and remains the real, working instance of this decision. Any future cross-reference to a Task (e.g. from a Dependency link, once those render as more than plain text) is a legitimate place to add this affordance — the target's own detail page's header is not.
- **D1.4-9** (decided 2026-08-30)<br>
  **Question:** Does Level 1 need a committed end-to-end/browser-automation test suite for the GUI, beyond the manual testing already planned (§7)?<br>
  **Decision:** no — deferred to Level 2. Level 1 relies on manual testing against the real running `rest-api` (§7) for each screen as it's built; automated browser-driven regression coverage is worth building once release cadence and screen count justify the investment. Recorded as a deferral in `Claude/Level2_Implementation/Scope.md`.
- **D1.4-10** (decided 2026-08-30)<br>
  **Question:** Cross-window drag-and-drop (e.g. dragging a Task from one open window onto a Project in another) is technically harder than in-window drag-and-drop, since native HTML5 drag-and-drop doesn't reliably cross separate top-level browser windows — should Level 1 build it, and how should that be decided?<br>
  **Decision:** not decided in the abstract — run a small, scoped spike during Stage 2 (§6.2) implementing exactly one such interaction (dragging a Task onto a Project across two open windows, reusing the `useReparentTask()` mutation `D1.4-7` already establishes) to learn the real technical issues first-hand. If it lands cleanly, build it out properly as part of Stage 2; if it doesn't, defer it to Level 2 and record that in `Claude/Level2_Implementation/Scope.md` at that time.<br>
  **Revised (2026-09-12):** the spike's own interaction was changed before it was run — from reparenting (Task onto Project) to creating a Dependency via Ctrl-drag, mirroring V1.2's real Ctrl-drag-creates-a-Dependency convention (`Requirements/UserInterfaceWindows.md` §4) rather than a Move-effect reparent. See §6.2 for the full revised shape.<br>
  **Outcome (2026-09-12):** the spike landed cleanly. Native HTML5 drag-and-drop was confirmed, by hand, to fire correctly across two separate `window.open()`-created standalone Task Detail windows — the specific unverified assumption this decision existed to test. Built out for real as part of Stage 2, not deferred: full detail, including exactly what was built and verified, is in §6.2's own "Outcome" paragraph; the manual walkthrough is §7 item 13.
- **D1.4-11** (decided 2026-08-30)<br>
  **Question:** How does the GUI reach the REST API during Stage 1 development, given a direct browser request from the Vite dev server's origin to the API's origin is genuinely cross-origin and fails on CORS, and API-side CORS configuration would be dev-only scaffolding to remember and later remove?<br>
  **Decision:** `gui-client/vite.config.ts`'s dev server proxies `/api/*` to the REST API (prefix stripped), and `VITE_API_BASE_URL` (`.env.example`) is set to the relative `/api` — the browser only ever talks to its own origin, so no CORS configuration is needed on the API at all. This deliberately mirrors `D1.6-4`'s Caddy routing (`/api/*` → rest-api, prefix stripped) exactly, so the same base URL and the same relative-path assumption carry through unchanged from Stage 1 all the way to production — not a dev-only workaround to revisit later.
- **D1.4-12** (decided 2026-08-30)<br>
  **Question:** What can be done to make the GUI's visual theme (fonts, colours, logos, icons) configurable, given the design isn't settled yet and is expected to change once there's something real to give feedback on?<br>
  **Decision:** centralise every visual value in one file, `gui-client/branding.json` — colours, font family, logo/favicon paths, app name — with the MUI theme (`src/theme/theme.ts`), the shared `Logo` component, the PWA manifest (`vite.config.ts`, `D1.4-5`), and `index.html`'s favicon/theme-color/title (via a small `transformIndexHtml` plugin) all reading from it rather than holding their own copies. Deliberately build-time only (edit the file, rebuild) rather than a runtime theme editor — a settings UI and persisted per-Organisation theme choice is real additional engineering a single-organisation Level 1 Demonstrator doesn't need; revisit if multi-tenant branding becomes a genuine Level 2/3 requirement, since a build-time config can't serve two Organisations' branding from one running deployment. Full detail: §3.9.
- **D1.4-13** (decided 2026-08-30)<br>
  **Question:** Should each V2 screen's layout be designed fresh from the use cases alone, or should V1.2's actual screen layouts inform it, given V1.2's UI is out of scope to port directly (`Scope.md` §4) but represents a tried-and-tested arrangement refined by real usage?<br>
  **Decision:** treat V1.2's real layouts — the WinForms `.Designer.cs` and WPF `.xaml` files under `V1.2/Apps/ProjectPal/ProjectPal/` — as a strong reference for each screen, not a constraint to reproduce exactly. They're worth reading before designing each screen for field grouping/order/emphasis, but V1.2 is WinForms-era technology from some years ago, and a modern web/MUI pattern that serves the same goal better wins over faithfully copying the old arrangement. Full detail: §5.
- **D1.4-14** (decided 2026-08-30)<br>
  **Question:** Task Detail shows no Start/End dates at all, even though V1.2's equivalent screen did — should V2 add stored date fields, or compute them client-side the way `D1.4-6`'s Urgency/Gantt-derivation carve-out already anticipates?<br>
  **Decision:** compute them client-side. Neither V1.2 nor V2 ever stores a Task's dates — both store only a business-day offset from the Project's start date (`8_ValidationAndVerification/Plan.md` §4.1) — and V1.2's own GUI computed `EarliestStartDate`/`StartDate`/`Duration`/`EndDate` for display every time (`V1.2/Libs/DBProjectPal/DBProjectPal/Task.cs`), never persisting them. `gui-client/src/lib/schedule.ts` reproduces this now (bounded to one level of predecessor-Dependency awareness for Stage 2; Stage 3's Gantt view extends the same module for the full recursive graph walk it needs, rather than duplicating it). Confirms `D1.4-6`'s carve-out was correctly scoped, and generalises it: before assuming a value must come from an API field, check whether V1.2 ever actually stored it either. Full detail: §3.8.<br>
  **Superseded by:** `D1.5-2` (`5_UrgencyCalculation/Plan.md`) — the "bounded to one level" scope this decision accepted for Stage 2 is upgraded to the full recursive graph, ahead of Stage 3 rather than as part of it.
- **D1.4-15** (decided 2026-08-30)<br>
  **Question:** Do V2's dropdown lists (Priority, Status, Task Type) need to match V1.2's option order, or is the database enum's declaration order sufficient?<br>
  **Decision:** match V1.2's order — `GUITaskColumns.cs`'s `GetComboValues_static` populates each dropdown in a specific, deliberate order (most-urgent-first for Priority; a particular non-alphabetical order for Status and Task Type), not schema-declaration order, and V2's `types.ts` constants were, before this was caught, using the latter. Fixed to match V1.2 name-for-name for Status and Task Type (identical value sets); Priority's five V1.2 names don't match V2's five 1:1, so it's ordered most-urgent-first by position instead, with the exact historical correspondence flagged for confirmation (`Q1.8-4`) rather than assumed. Owner/Requestor/Resources dropdowns are also now filtered to active People only, matching V1.2's `Person.AllActiveInstances`. Full detail: §3.10.
- **D1.4-16** (decided 2026-08-31)<br>
  **Question:** V1.2's Effort Type value "ManDays" is gendered language — what should V2 call it instead?<br>
  **Decision:** "PersonDays" (displayed as "Person Days"), paired unchanged with "Duration". Since Level 1 hasn't deployed anywhere yet, this was made as a direct schema-and-seed-data edit and full local rebuild (`Claude/Guidelines/ImplementationApproach.md` §5), not an additive migration — `database/migrations/001_initial_schema.sql`'s `effort_type` enum, `database/seed/001_example_data.sql`'s seeded Tasks, `gui-client`'s `EFFORT_TYPES` constant and the Effort-type toggle, and `Requirements/KeyConcepts.md` §10 all changed together. V1.2's own historical use of "man-days" is left as-is in V1.2's own (frozen, reference-only) source and in places that describe V1.2's actual behaviour — only V2's own terminology changed.
- **D1.4-18** (decided 2026-09-03)<br>
  **Question:** The Resources checklist (§3.11) is one of the pick-lists to be bound to a small scrolling area — what ordering and sizing rules should that box actually follow?<br>
  **Decision:** a two-phase sort — checked entries first (alphabetical within that group), then unchecked entries (alphabetical within that group) — recomputed from current state on every render, so ticking/unticking a resource immediately re-sorts it; this keeps whatever is actually assigned visible without scrolling regardless of list length. Displayed names are truncated to a fixed maximum (10 characters, easy to revisit) with the full name available as a hover tooltip, and the box is sized as a fixed-width, fixed-height control (5 rows visible before scrolling) rather than growing to fit its content. Implemented in `gui-client/src/features/tasks/TaskDetailPage.tsx`'s Resources panel. Full detail: §3.11.
- **D1.4-19** (decided 2026-09-03)<br>
  **Question:** A round of hands-on feedback against the Claude Design mockup (§3.11) asked for several further Task Detail field-layout changes — should these be ported into the real `gui-client` build, and how?<br>
  **Decision:** yes, ported into `TaskDetailPage.tsx` directly (not deferred pending `Q1.4-17`'s broader layout-direction decision, since these are self-contained field-level changes, not the header/tabbed-card chrome redesign that question is still about):
    - **Resources sits beside its related field rows, not in its own card.** V1.2 packs other fields into the same visual row as its Resources listbox (§3.11's existing principle); applied here by placing the (already fixed-size, D1.4-18) Resources box to the left of the Priority/Status/Task Type/Requestor/Owner row and the Effort/Effort Type/% Allocation/dates row, stretched to roughly their combined height (`flex` + `maxHeight`, not V1.2-style pixel-exact alignment — that level of precision was specific to the static mockup's fixed-size canvas, not a fluid responsive page).
    - **Project and Component get a wide row of their own**, separate from the short fixed-width fields (Priority etc.) — their values can run long (V1.2 screenshot).
    - **Effort Type is two stacked radio buttons** (`RadioGroup`/`Radio`, labelled "Days"/"Duration"), replacing the earlier `ToggleButtonGroup` — narrower, and matches V1.2's own control for this choice.
    - **Requested Start Date is now a real editable date input**, not read-only text, replacing the separate raw "start offset in days" number field entirely. This matches V1.2's actual UX (its own Requested Start Date control is what a user edits — V1.2 back-computes the stored offset from the chosen date and the Project's start date, not the other way around). `gui-client/src/lib/schedule.ts` gained `businessDaysBetween()`, the exact inverse of the existing `addBusinessDays()`, to do this back-computation.<br>
  **Full detail:** §3.11; implementation in `TaskDetailPage.tsx` and `lib/schedule.ts`.
- **D1.4-20** (decided 2026-09-03)<br>
  **Question:** `Claude/Requirements/V1.2_ScreenShots/TaskDetail_with_Calendar.png` shows V1.2's Requested Start Date *and* End Date fields both with a calendar-dropdown picker — checking `V1.2/Apps/ProjectPal/ProjectPal/Tasks/TaskDetail.Designer.cs` found actual `DateTimePicker` controls wired to both (`dateTimePickerRequestedStartDate`, `dateTimePickerEndDate`), not just Requested Start Date as `D1.4-19`/`D1.4-14` had assumed — only Planned Start Date has no picker in the designer (matching it being purely computed). Does V2's End Date need to become editable too, and if so how, given `8_ValidationAndVerification/Plan.md` §4.1 and `D1.4-14` established that neither version stores an actual End Date column?<br>
  **Decision:** yes — corrects `D1.4-14`/`D1.4-19`'s incomplete picture of V1.2's actual behaviour, found by reading `V1.2/Libs/DBProjectPal/DBProjectPal/Task.cs`'s `EndDate` setter (not just its getter, which is all the earlier research had checked): it back-computes an implied Start Date (`EndDate − (⌈Duration⌉−1)` business days) and assigns that to `StartDate`, whose own setter converts it to the same `m_startRelativeDaysToProject` offset `EarliestStartDate`/`D1.4-19` already write to — confirming End Date was never a second stored value in V1.2 either, just a second *entry point* to the one offset, anchored from the other end. V2's End Date field is now `type="date"` (real editable input, was read-only text) with an `onChange` reproducing that exact back-computation (`addBusinessDays(chosen, -(Math.ceil(duration)-1))` then `businessDaysBetween(project.start_date, impliedStart)`), while Planned Start Date stays read-only, matching V1.2's designer exactly. Separately, this also closes out the actual ask that prompted the research: both HTML `type="date"` inputs already render a native calendar-dropdown icon/mini-picker in every browser, with no extra component needed — so Requested Start Date (already `type="date"` since `D1.4-19`) already had this, and giving End Date the same input type was sufficient to give it the same picker V1.2 shows.<br>
  **Full detail:** implementation in `TaskDetailPage.tsx`, `lib/schedule.ts`.

- **D1.4-21** (decided 2026-09-04)<br>
  **Question:** A real Person can be known by a shorter name within a Team than their recorded name (e.g. "Alice" rather than "Alice Chen") — should V2 support this, and if so, how does the GUI resolve which name to show?<br>
  **Decision:** yes — `person_role.nickname` (nullable text, `D-DM-11`), scoped per-(Person, Team) since the same Person could be known differently on different Teams. Wherever a screen's data belongs to a Team, its display prefers that Team's nickname over the Person's plain name, falling back to the name when no nickname is set or when the name shown isn't actually scoped to one Team (Requestor still lists active People org-wide, `D1.4-15` — a candidate who isn't a member of the task's own Team just shows their plain name; Owner was later narrowed to the Team, see `D1.4-22`). Level 1 is read-only: only ever set via seed data (`gui-client/src/lib/people.ts`'s `personDisplayName()` reads it; nothing writes it). Editing is deferred to Level 2 — almost certainly a dedicated Team Management screen, since it's a team-lead-facing capability (reusing the same `_require_admin_or_team_lead` check `person_role`'s other writes already use), not something to scatter an inline edit control for across Task/Project/Component Detail. Recorded in `Claude/Level2_Implementation/Scope.md`.<br>
  For Remarks/Attachments, whose shared panels (`RemarksPanel`/`AttachmentsPanel`) take a generic `owner` (a Task, Project, or Component) rather than a Team directly: the calling page resolves and passes `teamId` down as a prop (its own Project's `team_id`, or — for a Task — that Task's Project's `team_id`), rather than having the shared panels re-derive it themselves from `owner` each time; the caller already has this value in hand.<br>
  **Full detail:** `Claude/Requirements/DomainModel.md` `D-DM-11`; implementation in `database/migrations/001_initial_schema.sql`, `database/seed/001_example_data.sql`, `rest-api/app/routes/teams.py`, `gui-client/src/lib/people.ts`, `TaskDetailPage.tsx`, `RemarksPanel.tsx`.

- **D1.4-22** (decided 2026-09-05)<br>
  **Question:** `D1.4-15` had Owner list active People org-wide, same as Requestor — on review this doesn't reflect how a Task is actually owned: an Owner who isn't even a member of the Task's Team is a data-quality footgun, not a real scenario worth supporting. Should Owner narrow to the Team, and if so, to which Team-scoped set?<br>
  **Decision:** yes — Owner now lists exactly the same candidates as the Resources listbox (active People with an `is_resource` `person_role` on the Task's own Team), not the broader "any active Person" set Requestor still uses. Ordered alphabetically by each candidate's Team-scoped display name (`D1.4-21`'s `personDisplayName`) — unlike Resources' checked-first grouping (`D1.4-18`), Owner is a plain dropdown with a single current value, so a simple alphabetical sort is all that's needed. Requestor is unchanged (still `D1.4-15`'s org-wide active-People list) — nothing here suggested a Task's requestor must belong to the Task's own Team the way its owner and resources do.<br>
  Known gap, not addressed here: if a Task's stored `owner_person_id` refers to someone outside this narrower set (e.g. seed data drift, or a Team reassignment after the Owner was set), the `<select>` has no matching `<option>` and will render as if nothing were selected, even though the underlying field is untouched until the user picks something and saves. Not currently possible via the seeded data or the GUI itself, so left unhandled rather than adding speculative code for an unreachable case — revisit if a real path to that state appears (e.g. once Level 2 allows moving People between Teams).<br>
  **Full detail:** implementation in `gui-client/src/features/tasks/TaskDetailPage.tsx`.
- **D1.4-23** (decided 2026-09-12)<br>
  **Question:** `Q1.4-17` — which compact Task Detail layout direction (if any) should be built: "dense header + tabs" (closest to today's structure, just compacted), "metadata sidebar" (a two-column split separating identity/scheduling fields from content), or "accordion sections" (independently-collapsible sections, the most aggressive on default compactness)?<br>
  **Decision:** "dense header + tabs" — the direction `TaskDetailPage.tsx` was already built against (§3.11's compact identity header, the `subTab`-driven Dependencies/Attachments/Remarks strip). No implementation change from this decision; it confirms the build already matches the chosen direction rather than one of the other two mockups.
- **D1.4-24** (decided 2026-09-12)<br>
  **Question:** §6.3's original plan was to spike two MIT-licensed React Gantt libraries (`gantt-task-react`, `frappe-gantt`) and choose between them before building Stage 3. Should Stage 3 actually go that route?<br>
  **Decision:** no — build bespoke instead, porting V1.2's own from-scratch Gantt implementation (`V1.2/Libs/PlanDisplay/`) rather than adopting either library. Checked directly against npm: `gantt-task-react` is dead (last published 2022, pinned to React ^18, incompatible with this project's React 19); `frappe-gantt` is alive and mature but vanilla-JS with no native hierarchy support; neither supports V1.2's resource-loading swimlane (`ResourceLevel.cs`), which any choice would need bolting on regardless. V1.2's own positioning/stacking algorithms (`Constants.cs`'s day-to-pixel model, `ResourceLevel.cs`'s effort-accumulation stacking) are portable, framework-independent logic — a proven reference to port, not a design problem to solve fresh — consistent with `D1.4-6`'s existing preference for owning presentation logic in TypeScript rather than a third-party package. Full reasoning and scope: §6.3.<br>
  **Also corrected by this decision:** `Requirements/UserInterfaceWindows.md` §3.7 previously said the Plan Display recursively shows "Task/Project/Component" bars together — wrong, per a direct re-check of `Apps/ProjectPal/ProjectPal/GanttDisplayHelper.cs`: Components only ever appear in `ComponentWindow`'s own separate embedded Gantt tab (a parallel `Component → SubComponents` tree-walk), never inside the Project-scoped Plan Display. Fixed in `UserInterfaceWindows.md` §3.7/§3.8. Stage 3's pilot is scoped to the Project hierarchy only; a Component-scoped variant is Stage 4 reuse, not part of this pilot.
- **D1.4-25** (decided 2026-09-13)<br>
  **Question:** the placeholder `Dashboard` screen (a per-Resource workload report stub, never built out) was the "/" landing route for every role. What should a real login actually land on?<br>
  **Decision:** removed `Dashboard` entirely; "/" now redirects straight to `/tasks` (All Tasks) for every role. A `TeamLeadUser` should properly land on V1.2's `MainWindow` equivalent instead, but that screen doesn't exist in V2 yet — noted here as a follow-up: once a "MainWindow" landing screen is built, route a `TeamLeadUser` (`isTeamLeadOfAnyTeam`, `lib/people.ts`) there and leave every other role on `/tasks`.
- **D1.4-26** (decided 2026-09-13)<br>
  **Question:** the Gantt view had no month-boundary labelling at all — a user could see the faint monthly gridline (`D1.4-24` follow-up) but not which month it started. How should a "Mmm"/"Mmm-YY" footer label be positioned and kept in sync with the chart as it scrolls and zooms?<br>
  **Decision:** a dedicated footer row, centred under each month gridline, sharing the *same* horizontal scroll container as the chart itself rather than a `position: sticky` element pinned inside the chart's own scrolling pane. The sticky approach was tried first and looked correct, but a real browser quirk surfaced under headless-CDP testing: once a sticky child sits inside an `overflow: auto` element, the browser's own computed max `scrollTop` for that element stops accounting for the sticky child's full height, so the last row or two could still end up scrolled in behind the "pinned" footer with no way to scroll further to reveal it — reserving extra bottom space in the scrollable content did not fix this. Fixed by splitting the chart's two scroll axes across two nested elements instead: an outer element scrolls *only* horizontally, wrapping both the chart (which now scrolls *only* vertically) and the footer row (fixed height, no scroll of its own) at the same fixed width — the footer tracks horizontal scroll for free as an ordinary sibling in that shared horizontal-scroll box, with no JS syncing needed for that axis, and the outer element's own native horizontal scrollbar necessarily renders below both of them. Worth remembering generally: a `position: sticky` footer inside a bidirectionally-scrolling container is not a safe pattern in this codebase — prefer splitting the scroll axes across nested elements instead.

- **D1.4-27** (decided 2026-09-13)<br>
  **Question:** the Gantt view's row order was always the same fixed default (Tasks by start date, then sub-Projects alphabetically) — the user asked to be able to manually reorder rows for personal convenience, without that touching the database at all (never a real schema change, just a display preference). How should that ordering be stored and applied, and how far should a drag be allowed to move a row?<br>
  **Decision:** a client-side-only order override, `GanttCustomOrder` (`lib/ganttLayout.ts`) — a map from a parent's own key ("root" for a top-level Project, or `project:<id>`) to its children's desired key order, applied inside `collectRows`/`buildGanttLayout` itself (not a render-time re-sort in `PlanPage.tsx`) since sibling order determines each row's Y position, which the layout module — not the renderer — computes. A drag is scoped to `GanttBar`'s own new `parentKey` field: the dragged row's candidate drop positions are only ever compared against bars sharing that same `parentKey`, so a hierarchy/parent change is not just disallowed but structurally impossible from this calculation, not a validation check bolted on afterwards. Persisted to `localStorage` (`projectpal.ganttOrder.<personId>`, keyed per Person so a shared browser profile can't mix up two users' preferences) only when the new "Memorise order" button (below the label column) is clicked — dragging alone only changes the current session's view, matching "this change is local to use[r]" from the request. A Task and a sub-Project under the same parent are treated as one combined orderable list (not two separately-orderable lists) — dragging a Task above a sub-Project, or vice versa, is allowed, since visually they're just that parent's child rows.

- **D1.4-28** (decided 2026-09-13)<br>
  **Question:** a bar's hover tooltip used a plain SVG `<title>` (name + date range) — the user reported the cursor itself covers the tooltip's own first letter. Can a native title tooltip's position be adjusted?<br>
  **Decision:** no — a native `<title>` tooltip's position is entirely browser-controlled, with no CSS/JS hook to offset it. Replaced with a custom cursor-tracking tooltip (`BarTooltip` state in `PlanPage.tsx`), offset 15px right and 15px down from the cursor, updated on the bar's own `onMouseMove` (not just `onMouseEnter`) so it keeps tracking as the cursor moves across a wide bar. The `<title>` element itself is removed entirely, not left in place alongside the custom one (which would just show both).
- **D1.4-29** (decided 2026-09-13)<br>
  **Question:** the label column's own visible height didn't account for the space the *drawing* pane's month-footer row (`D1.4-26`) and its own native horizontal scrollbar take up — so the label list could show rows further down than the chart had any room left to draw their own bars for (hidden behind the footer/scrollbar). Screenshot: `PlanGantt_260913_2.png`. How should the two panes be kept in visual sync, and where should "Memorise order" (`D1.4-27`) live once that's fixed?<br>
  **Decision:** reserve the same bottom space on the label column that the drawing pane actually consumes — `MONTH_FOOTER_HEIGHT` (bumped 20px → 26px so it also comfortably fits a button on its own, not just month-label text) plus the horizontal scrollbar's own height, measured at runtime via `ResizeObserver` on `hScrollAreaRef` since a native scrollbar's height isn't a fixed, portable CSS value and only actually applies once the chart is wide enough to need one. "Memorise order" moves into that reserved strip, directly under the (now shorter) label list, rather than its own separate full-width row below the whole Gantt area — freeing that row's height for the Gantt area itself, which now simply extends to fill whatever vertical space is actually left on the page (no code change needed for that part beyond removing the now-unnecessary sibling row).

- **D1.4-30** (decided 2026-09-13)<br>
  **Question:** the user asked for a right-click-drag pan over the Gantt graphic area — hold the right mouse button and drag to scroll the chart on both axes, with no zoom change, "as if the scrollbars themselves had been dragged." How should this be implemented, and does it need the same anchor-preserving math the zoom features use?<br>
  **Decision:** no anchor math needed — unlike zoom (`D1.4-24` follow-up), nothing here changes scale, so "the date/task under the cursor stays the same" falls straight out of a plain scroll-position delta. A native (non-React) `mousedown`/`contextmenu` listener pair on `hScrollAreaRef` (the chart + month-footer area specifically, not the label column) starts a drag on the right button (`event.button === 2`): `window`-level `mousemove`/`mouseup` listeners apply `hScrollArea.scrollLeft`/`drawingArea.scrollTop` deltas directly from the cursor's own movement, and the browser's context menu is suppressed the standard way (`preventDefault()` on both the triggering `mousedown` and on `contextmenu` itself — the same technique map/canvas-editor UIs universally use for this exact interaction). Verified via headless CDP: a drag correctly produces an exact, matching scroll delta on both axes with zoom left untouched, and a plain (non-drag) right-click's `contextmenu` event confirms `defaultPrevented` — headless CDP could not conclusively reproduce the *combination* of a scroll-mutating drag immediately followed by release-triggered `contextmenu` suppression (a known hard-to-simulate area for synthetic input), but the underlying technique is standard and independently confirmed correct in isolation.

- **D1.4-31** (decided 2026-09-13)<br>
  **Question:** in the label column, only the actual `<text>` glyphs were hit-testable — hovering the gaps around/between labels (short text, indentation, the space below a row's baseline) fell through to the plain background, showing the browser's own default cursor instead of "grab" (a visible flicker as the mouse crossed glyph boundaries) and unable to start a drag at all from those gaps.<br>
  **Decision:** one `cursor: "grab"` set once on the label pane's own container (`labelAreaRef`), not per-`<text>` — inherited uniformly by everything inside, so there's nothing left to flicker between. Each row also gets an invisible, full-width `<rect fill="transparent">` spanning its own `ROW_HEIGHT` band (the trick: `fill="transparent"` is still hit-testable, unlike `fill="none"`, which excludes the interior from hit-testing entirely) carrying the row's own drag/double-click handlers; the `<text>` itself becomes purely visual (`pointer-events: none`), so every pixel in a row's band — not just its glyph ink — starts that row's drag. Verified via headless CDP: `elementFromPoint` resolves to the same `<rect>` (with the same computed `cursor: grab`) whether probed exactly on a glyph, well past a short label's own text, or in the vertical gap between two rows' baselines; a drag started from such a gap point (30px past "Customer Portal Refresh"'s own short text) still opens the floating drag label and reorders correctly.

- **D1.4-32** (decided 2026-09-13)<br>
  **Question:** following `D1.4-31`'s uniform-cursor fix, the user asked for four further label-column refinements: (1) a plain arrow cursor instead of the "grab" hand; (2) a visible dotted-border highlight on whichever row is currently under the cursor, so the full-row hit target is actually visible; (3) the same shared name control the graphical area's own bar-hover already populates (`hoveredLabel`), populated the same way while hovering the label list; (4) a tooltip showing a label's full text, ideally only when the column is too narrow to show it in full. How should the "is this actually truncated" check work, given SVG `<text>` has no `text-overflow: ellipsis` equivalent?<br>
  **Decision:** all four implemented together, all driven by the same per-row hit-rect's hover events (`D1.4-31`'s rect, not the text). (1) `cursor: "default"` replaces "grab" on the label pane's container. (2) A single dashed (`strokeDasharray="2,2"`) outline rect, redrawn at whichever row's own `hoveredLabelBar` state currently points to. (3) The same `setHoveredLabel(bar.hoverLabel)` call `GanttBarRect`'s own `onMouseEnter` already makes, now also called from the label row's hover — same state, same control, so there's exactly one source of truth for "what's shown there" regardless of which side of the pane triggered it. (4) Real truncation detection: a ref per rendered `<text>` node plus a `useLayoutEffect` calling the DOM's own `getComputedTextLength()` against the space actually available to it (`labelColumnWidth` minus its own depth-indent) — accurate (reads the real rendered glyph width, not an estimate) and re-runs whenever `layout`, `labelColumnWidth`, or `zoomY` (row/font scale) change. The tooltip itself reuses `D1.4-28`'s own floating-box mechanism (a native `<title>` still can't be repositioned) rather than reintroducing that same limitation for a second tooltip. Verified via headless CDP against the real seed data: a naturally long label ("Explore self-service reporting dashboard", clipped at the column's default width) shows the tooltip with its full text; a short one ("Invoice PDF export") shows none.

- **D1.4-33** (decided 2026-09-13)<br>
  **Question:** a tooltip (bar or label, `D1.4-28`/`D1.4-32`) could still pop up while some other drag was already in progress (row reorder, label-column resize, right-click pan) — clutter on top of whatever that drag's own feedback (the floating drag label, the resize itself, the panning chart) was already showing.<br>
  **Decision:** one shared ref, `isDraggingRef`, set `true` for the duration of any of the three drag interactions and checked before either tooltip setter shows a *new* tooltip (clearing one, i.e. setting it to `null`, is always allowed, so a tooltip already open when a drag starts still disappears immediately rather than lingering). A ref rather than state, matching this file's existing convention (`zoomXRef`/`zoomYRef`) for values only read inside event-handler closures, not rendered directly. Verified via headless CDP: a truncated label's tooltip shows normally at baseline, is suppressed while dragging a different row over it (the drag's own floating label stays visible throughout), and resumes working immediately after the drop.

- **D1.4-34** (decided 2026-09-13)<br>
  **Question:** `D1.4-32` centred each label's own text within its full `ROW_HEIGHT` row band — but the *bar* it belongs to was still drawn top-aligned to that same row (its own, shorter `BAR_HEIGHT`), so the two drifted apart: a bar's own centre now sat noticeably above its label's centre. Given the user is happy with the bar's own height, how should the two be brought back into alignment?<br>
  **Decision:** move the bar, not the text — the label stays centred on the row (already correct per `D1.4-32`); every bar-vertical-position calculation in the renderer now adds a `barVerticalOffset = ((ROW_HEIGHT - BAR_HEIGHT) / 2) * scaleY`, centring the (still `BAR_HEIGHT`-tall) bar within the row band instead of pinning it to the top. Applied to the bar rect itself, the extent guide-lines' own start/end points, and the dependency-arrow endpoints (all of which anchor to "the bar's own position") — but *not* to a Boxed-mode Project's own box (`GanttBarRect`'s `isBoxedProject` branch), which spans multiple rows and stays top-aligned to its own row on purpose, a different visual convention from a single-row bar. Verified via headless CDP: a bar's own rendered vertical centre now sits at the exact same y-coordinate as its label's own centre, 0px offset, with the bar's own height unchanged (still 10px at 100% zoom).

- **D1.4-35** (decided 2026-09-13)<br>
  **Question:** a new "Weekends" checkbox (top controls row, unchecked by default) should exclude Saturdays/Sundays from the chart's own day-to-pixel mapping entirely — 5 units between consecutive weekly gridlines, not 7 — with a bar starting on a weekend drawn as if it started at the beginning of the following Monday, and one finishing on a weekend drawn as if it finished at the end of the preceding Friday. How should the start/end clamping be implemented, given it has to apply consistently to bars, gridlines, the today-marker, and dependency arrows?<br>
  **Decision:** one new pure function, `compressedDayOffset(from, to)` (`lib/ganttLayout.ts`) — the count of weekdays (Mon–Fri) in the half-open range `[from, to)`. This single formula produces the requested clamping with no separate clamping step at all: since neither a Saturday, the Sunday right after it, nor the gap up to the *following* Monday contains any weekdays, `to` being any of Sat/Sun/next-Monday all yield the identical result — so a bar "starting" on a weekend already lands at the same offset a Monday start would, and one "ending" on a weekend already lands at the offset the *preceding* Friday's end would, both for free. `buildGanttLayout` picks this or the existing `calendarDaysBetween` via one local `dayOffset` helper depending on the new `excludeWeekends` parameter, applied to bar x/width (both ends measured from the shared `minDate` reference then subtracted, avoiding an asymmetry that computing width directly from `compressedDayOffset(startDate, endDate)` would introduce when `startDate` itself is a weekend) and to `todayX`; dependency arrows need no separate change, since they already just reference their bar's own (now-correct) x. `computeGridLines` takes the same flag, walking real calendar days but advancing its own position counter only on a weekday, so Monday/1st-of-month are still found by their true dates while landing 5-apart instead of 7. Verified: 26 unit tests (the offset function's own clamping/continuation behaviour, gridline spacing both ways, and one `buildGanttLayout` integration case) plus a live headless-CDP check against real seed data — week-line spacing measured exactly 50px (5 days) unchecked and 70px (7 days) checked, with the checkbox itself defaulting to unchecked.

- **D1.4-36** (decided 2026-09-13)<br>
  **Question:** toggling "Weekends" (`D1.4-35`) changes the chart's own day-to-pixel mapping, so without any compensation the visible content jumps — the same date no longer sits at the same x. The user asked that the date sitting at 25% from the left (the same fraction the "Today" button itself uses) stay fixed at that screen position across the toggle. This needs an inverse of `compressedDayOffset` (a pixel position back to a real date) — but that forward function is many-to-one at a weekend boundary (Sat/Sun/next-Monday all produce the same offset), so what should its inverse return for such an offset, and does this interact with anything else already scrolling the view?<br>
  **Decision:** `dateAtCompressedOffset(from, units)` (`lib/ganttLayout.ts`), the exact inverse, canonically resolves an offset only a weekend forward-maps to as the *following Monday* — the nearest date that actually occupies real, visible width there, since the compressed timeline has no pixel space of its own for a weekend at all. The checkbox's own `onChange` reads the date currently at 25% under the *old* mapping (via this inverse) into a ref before flipping state; a `useLayoutEffect` keyed on the checkbox's own state then re-derives that same date's *new* x under the freshly-committed layout and restores `scrollLeft` — paint-blocking, so it applies before the browser shows the jumped frame. Surfaced a real, separate bug while wiring this up: the existing "auto-scroll to today" effect was keyed on `[layout]` and fired on *every* layout recomputation, not just a fresh Plan-view's first one — including a "Weekends" toggle (or a manual row reorder, `D1.4-27`, which also rebuilds `layout`) — silently clobbering this new restore immediately afterward (a plain `useEffect` runs after a `useLayoutEffect` from the same commit). Fixed by gating it behind `hasAutoScrolledRef`, reset only when `projectId` itself changes (a real navigation to a different scope), not on every incidental `layout` rebuild. Also fixed the pre-existing date-under-cursor control (`D1.4-26`) to invert whichever mapping is actually active — it had always used a raw calendar-day add regardless of the "Weekends" checkbox, silently showing the wrong date once weekends were excluded. Verified via live headless CDP: hovering the same screen position (25% from the left) before toggling, after toggling on, and after toggling off again all read back the identical date, with the scroll position adjusting each time (300px → 496px → 296px, the small residual difference being expected day-granularity rounding, not drift).

- **D1.4-37** (decided 2026-09-13)<br>
  **Question:** a bar's width was `endX − x` (both ends measured as the *start* of their own day), so a task starting and finishing the same day rendered as `endX === x` — a near-zero sliver, floored to a 1-*pixel* minimum rather than a meaningful width. The user asked that a bar span from the beginning of its start day through the end of its end day, so a same-day task reads as one full day wide.<br>
  **Decision:** `endDate` is a whole day the bar occupies, not an instant it stops at — so the right edge needs to reach the *start of the day after* `endDate`, not `endDate`'s own start. Fixed by adding one calendar day to `row.endDate` (`addCalendarDays(row.endDate, 1)`) *before* running it through the same `dayOffset` mapping `D1.4-35` already uses for the start edge, rather than adding a flat `PIXELS_PER_DAY` to the final pixel width afterwards — doing it this way (nudging the date, then mapping) is what keeps it correct under "Weekends" exclusion too: an `endDate` of Friday needs to reach Saturday's own (weekend-clamped) offset, which is exactly what `compressedDayOffset` already gives for a Saturday `to` — not simply "Friday's own offset plus one unit" computed independently, which would double up with the exclusion logic. Dependency arrows and `chartWidthBase` needed no separate change, since both already just reference a bar's own `x`/`width`. Verified: 3 new unit tests (a same-day task is exactly `PIXELS_PER_DAY` wide; a 3-calendar-day task is `3 × PIXELS_PER_DAY`, not `2 ×`; a task ending Friday is the same width with weekends shown or excluded, since its own span contains no weekend to compress away) plus a live headless-CDP sweep across 12 real bars from the seed data — every one's rendered width matched `(inclusive day count) × 10px` exactly, including a genuine same-day task ("Fix payment webhook race condition", 29-Jan → 29-Jan) which now renders as a full day rather than a sliver.

- **D1.4-38** (decided 2026-09-13)<br>
  **Question:** the user reported a real Task (#12354, "AAA") whose own bar rendered visibly one day later than its own tooltip claimed ("08-Jan-26 → 08-Jan-26") — the date-under-cursor control read "09-Jan-26" across the *entire* bar's width, only in the top-level ("Weekends" unchecked) view, never in a hand-built test. Both the first live re-check and a synthetic reproduction using the task's own real data (in a project-scoped view) failed to reproduce it — what made the top-level view different?<br>
  **Decision:** a real bug in `compressedDayOffset` itself (`D1.4-35`), found only by reproducing against the *actual* full dataset (fetched live from the API and run through the real `buildGanttLayout`) rather than hand-built fixture dates. `compressedDayOffset` compared raw timestamps (`cursor.getTime() < to.getTime()`); a computed schedule date can carry a non-midnight time-of-day component — confirmed in this exact data: Project 10191's `start_date` ("2023-09-12") parses as UTC midnight, which is 01:00 local during British Summer Time, and `addBusinessDays`' repeated `setDate` calls preserve whatever time-of-day a date starts with, so that stray hour (compounding to 2 hours by the time it reached Task 12354's own computed date, `08-Jan-2026 02:00:00`) rode along all the way to the final result. Once `to` sits even a minute past local midnight, a raw timestamp comparison counts `to`'s own calendar day as one *extra* iteration, drawing the bar one compressed day late — invisible in every hand-built test so far, which only ever used exact-midnight dates. `calendarDaysBetween` (`lib/schedule.ts`) already guards against exactly this by comparing `calendarDayNumber` (a UTC-day-index reconstructed from local calendar fields, discarding time-of-day) rather than raw timestamps; `compressedDayOffset` now does the same. `dateAtCompressedOffset` (`D1.4-36`) needed no change — it never compares against a `to` date at all, just counts weekday transitions from `from`, which is immune to this by construction. Verified: a new unit test with a deliberately non-midnight `to` (2am, noon) confirms it now matches the midnight case; a full reproduction against the live dataset (all real Projects/Tasks/Dependencies fetched from the running API) confirms Task 12354's bar now lands on 8-Jan exactly, both via direct computation and a live headless-CDP hover sweep across its entire width.

- **D1.4-39** (decided 2026-09-13)<br>
  **Question:** `D1.4-25` removed the placeholder Dashboard entirely and left a real MainWindow-equivalent landing screen (§5's MainWindow row) as an unscoped follow-up — is building a real one in scope for Level 1?<br>
  **Decision:** yes — in scope for Level 1, added to Stage 4 (§6.4). Its actual content (what a landing dashboard should show) isn't designed yet; that happens when Stage 4 reaches it. Distinct from the secondary per-Resource workload-over-time chart (`D1.4-40`, deferred to Stage 6) — that's a richer, Gantt-adjacent visualization, not this simpler landing summary. Once built, `D1.4-25`'s own routing follow-up applies unchanged: a `TeamLeadUser` lands there, every other role continues to land on `/tasks`.

- **D1.4-40** (decided 2026-09-13)<br>
  **Question:** Stage 3 (§6.3) left three items explicitly deferred with no assigned stage of their own (the resource-loading swimlane, the People/Status/Priority colour-mode toggle, the secondary per-Resource workload-over-time chart), and Stage 4 (§6.4)'s own windows each carry real V1.2 drag-and-drop interactions (Project/Component reparenting, moving a Task by dragging it onto a Project/Component row, OS-file drag onto Attachments, cross-window Ctrl-drag Dependency creation) that Stage 2's `D1.4-4`/`D1.4-7` precedent would otherwise replace with explicit controls one window at a time, with no single place tracking that they're all the same *kind* of deferred work. How should the Build Order (§6) account for both?<br>
  **Decision:** two new stages. **Stage 6 ("Polishing," §6.6)** collects the three unassigned Stage 3 items — reasons: risk reduction (the more architecturally demanding/foundational work in Stages 3–5 should land first) and better-informed design (some of these are hard to get exactly right until more of the rest of the system exists to judge them against). The Component-scoped Gantt variant stays assigned to Stage 4, not moved here — it's a straightforward reuse of already-built rendering code once Component Detail exists, not a genuinely risky or hard-to-judge-in-advance item like its three siblings. **Stage 5 ("Drag and Drop," §6.5)** collects every Stage 4 window's own drag-and-drop interactions, deferred there individually (each window keeps Stage 2's explicit-control pattern for Level 1's own first cut) rather than built alongside that window — Stage 5 needs those windows to actually exist first, the same real prerequisite `D1.4-10`'s own cross-window spike had (it needed two already-open Task Detail windows to drag between; a Project Detail ↔ Project Detail or Task ↔ Project Detail drag needs the same, once real). Both deferrals are provisional, not final: an item may be pulled forward into an earlier stage later if it turns out straightforward to build alongside the window/feature it was deferred from, rather than waiting.

- **D1.4-41** (decided 2026-09-14)<br>
  **Question:** `ProjectDetailPlan.md` (this folder) found that V1.2's `Project.IsActive` (`V1.2/Libs/DBProjectPal/DBProjectPal/Project.cs`) is a computed property — Priority not Cancelled/Closed, *and* at least one direct Task that isn't Closed/Cancelled or an active sub-Project (recursively) — not a stored column, and that the already-shipped Gantt view (`lib/ganttLayout.ts`'s `isVisibleProjectPriority`) currently only implements the Priority half of it as a stand-in. Porting the full definition (as `isProjectActive`, `lib/schedule.ts`) is needed for Project Detail's own tree and the future Project List's "Is Active" column/default filter regardless — should switching the Gantt view over to the same, now-fuller function happen as part of that same work, or separately?<br>
  **Decision:** separately — deferred to Stage 6 (§6.6), not bundled into Project Detail's own build. `isProjectActive` is written once, now, as part of building Project Detail (`ProjectDetailPlan.md` §5.4), but switching `lib/ganttLayout.ts` over to it is a real behaviour change to already-shipped, tested code (it can change which Projects appear in `/plan`'s default view for real data — a Project with an acceptable Priority but no open Task and no active sub-Project would newly disappear), unrelated to Project Detail's own, separate build — the same reasoning already applied to keep Stage 4/5/6 cleanly separated (`D1.4-40`).

- **D1.4-42** (decided 2026-09-14)<br>
  **Question:** `ProjectDetailPlan.md` §4.4 proposed deferring the sub-Project/Task tree's own per-row task-count/priority summary and per-row "Gantt Display" shortcut button to Stage 6, rather than building them as part of Project Detail's own Stage 4 work — confirm or reject?<br>
  **Decision:** confirmed — deferred to Stage 6 (§6.6). Added to that stage's own list alongside its other deferred items.

- **D1.4-43** (decided 2026-09-14)<br>
  **Question:** `ProjectDetailPlan.md` §4.7 originally proposed "Add Task" as an instant, silent create (a placeholder description, then straight into Task Detail for full editing) — matching V1.2's apparent lack of a separate "New Task" dialog window. The user asked whether V1.2 actually has a minimum-required-fields rule for creating a Task or Project, since if so, an instant silent create isn't a faithful port and a small dialog is needed instead. Investigated directly against `V1.2/Apps/ProjectPal/ProjectPal/Tasks/TaskDetail.cs` and `Projects/ProjectDetail.cs`/`ProjectWindow`'s `NewProject` flow.<br>
  **Decision:** yes, V1.2 does enforce required fields at creation time, and V2's design changes to match. Full findings and the revised design are in `ProjectDetailPlan.md` §4.6/§4.7 (updated) — in short: **Task** creation (`TaskDetail.cs`'s `buttonOK_Click`, gated by `WindowMode.New`) refuses to save without a Component, Project, Description, Priority, Task Type, and Requestor, checked in that order with a specific error message each ("A Component must be specified", "The task must have a Description", etc.); opened from a Project's own context (as Project Detail's "Add Task" would), Project/Priority/Requestor already arrive pre-filled with sensible defaults (the owning Project, "Med", and the creating Person respectively) and only Description, Component, and Task Type are genuinely left blank and must be actively supplied. **Project** creation (`ProjectWindow`'s `toolStripButton1_Click`, via the `NewProject` dialog) only requires a Name — nothing else — which the existing Create/Rename dialog design (§4.6) already collects, so no change was needed there. V1.2 has no equivalent re-validation when *editing* an already-existing Task or Project (the `WindowMode.New` guard only ever applies to a brand-new one) — not changed here, out of scope for this screen.

- **D1.4-44** (decided 2026-09-14)<br>
  **Question:** `ProjectDetailPlan.md` §5.5 found that `errors.py`'s DB-constraint exception handlers return `{"error": "<message>"}`, while `formatApiError` (`lib/apiErrors.ts`) — the function every mutation's own catch block already relies on to show a real server message — only reads a `{"detail": "<message>"}` body, the shape every `HTTPException`-raised error in this codebase actually produces (`errors.py` registers five handlers over `psycopg.errors.*` — `RaiseException`/`UniqueViolation` → 409, `ForeignKeyViolation`/`NotNullViolation`/`CheckViolation` → 400, plus a catch-all `psycopg.Error` → 500 — all sharing one `_error(message, status_code)` helper that built `{"error": message}`). Fix by changing `errors.py` to match every other error in the codebase, or by widening `formatApiError` to read both shapes?<br>
  **Decision:** changed `errors.py`'s `_error()` helper to build `{"detail": message}` instead of `{"error": message}` — one response shape for every error in the API, not two. `lib/apiErrors.ts`'s `formatApiError` needed no change at all (it already only ever read `.detail`, which is exactly what made the old `{"error": ...}` shape silently fall through to its generic fallback text). One existing test asserted the old shape directly (`tests/test_dependency_rules.py::test_dependency_cycle_is_rejected`, checking the dependency-cycle rejection message) — updated to read `.json()["detail"]`. Verified: rebuilt the `rest-api` Docker image (`docker compose up -d --build rest-api` — the container has no live source mount, so the on-disk edit alone doesn't reach it) and ran the full `pytest` suite against it: 51 passed, 0 failed.

- **D1.4-45** (decided 2026-09-14)<br>
  **Question:** should the new "Add Task" dialog (`ProjectDetailPlan.md` §4.7) require a Component before creating a Task, matching V1.2's own creation-time check exactly (`TaskDetail.cs`'s `WindowMode.New` validation, `D1.4-43`), or make it optional, matching `DomainModel.md` §2.6's own documented "Component is optional" stance? No sentinel "Not Set" Component exists in V1.2, `V1`, or `Original` to sidestep the choice (checked and ruled out exhaustively — `ComponentId`/`AffectedComponentId` is a plain nullable foreign key in every version, with nothing resembling `Person.UnassignedResource`'s own real sentinel for Resource).<br>
  **Decision:** required — matching V1.2 exactly. `ProjectDetailPlan.md` §4.7's Component field is a required `FieldTreePicker` (no `allowNone`), and the "Add Task" dialog's own validation refuses to create the Task without one selected, alongside Description and Task Type.

- **D1.4-46** (decided 2026-09-14)<br>
  **Question:** implementing Project Detail per `ProjectDetailPlan.md` required generalising `DependenciesPanel.tsx` (§5.2) from a Task-only `taskId` prop to an owner that can be either a Task or a Project, so its "Add Dependency" dialog can search and link both. While verifying this against real seeded data (a cross-Team pick the caller has no rights on), a pre-existing gap surfaced: `handleAdd` called `createDependency.mutateAsync(...)` with no `try`/`catch` at all — a rejection (`dependencies.py`'s `create_dependency` requires being owner-or-`TeamLeadUser` on *both* sides) became an unhandled promise rejection, and the "Add Dependency" dialog just sat there with no visible feedback, on Task Detail's own long-shipped Dependencies tab as much as on Project Detail's new one. Fix now, or leave for a separate pass?<br>
  **Decision:** fixed now, as part of this same generalisation, rather than shipping (or re-shipping, for Task Detail) a screen with a known silent-failure path. `handleAdd` now wraps its mutation in `try`/`catch`, showing the real server message (`formatApiError`) inline in the dialog rather than closing/doing nothing; the "Add" button is disabled while the mutation is in flight. Verified via headless CDP against real seeded data: a same-Team Project↔Project Dependency now creates, appears in the right list, and deletes cleanly; a cross-Team pick now shows "Requires being the owner (above ReadOnly) or TeamLeadUser on this Team" directly in the dialog instead of failing silently. Confirmed Task Detail's own Dependencies tab is otherwise unaffected (`Task #2`'s real predecessor/successor still resolve and display correctly).

- **D1.4-47** (decided 2026-09-18)<br>
  **Question:** after Project Detail (`ProjectDetailPlan.md`) shipped, the user asked for two refinements: (1) initially reported as "the Top Level Projects window should only show Projects, never Tasks" — investigated and corrected by the user themselves once shown that this is already governed entirely by the existing Task-visibility filter (None/Open/All), not a separate mode-based rule — the real ask underneath was just to change that filter's own **default** from "Open" to "None"; (2) sibling Projects at the same level (`ProjectTaskTree.tsx`'s `childProjectsOf`) should sort by Priority (highest first) then alphabetically, not alphabetically alone.<br>
  **Decision:** both implemented. The Task-visibility filter's default (`ProjectDetailPage.tsx`) changed from `"Open"` to `"None"` — the filter itself, and its effect at every depth and in both "Top Level Projects" and a specific Project's own view, is unchanged; only the initial value differs. Sibling Project ordering now sorts by `priorityWeight` (`lib/schedule.ts`, exported for this — the same High=5…Cancelled=-1 mapping Urgency already uses, not a second one) descending, then by name — applied uniformly wherever sibling Projects are listed (`ProjectTaskTree.tsx`'s `childProjectsOf`, now taking `parentId: number | null` so the top-level case reuses the exact same function instead of its own separate alphabetical-only sort). Verified via headless CDP against real seeded data: "Top Level Projects" opens with Tasks hidden by default and Projects ordered High/MedHigh-tied-then-alphabetical-then-lower; toggling to "All" reveals Tasks at every depth, including inside an expanded top-level Project; a nested sub-Project pair (Database Migration, MedHigh; API Layer, Med) render in that Priority order, not alphabetical.

- **D1.4-48** (decided 2026-09-18)<br>
  **Question:** the user asked for a reusable `TaskGrid` component (factored out of All Tasks) that other windows can embed, configurable per window for which columns it shows and whether its filter row is shown, plus inline cell-editing on some columns. Should "what's editable" be decided per embedding window, or centrally?<br>
  **Decision:** centrally, in one place, used identically by every window — not a per-window setting, and not something `TaskGrid` accepts an override for. Full design in `TaskGridPlan.md` (this folder); in short: `lib/permissions.ts` gains `canEditTaskField(person, teamId, ownerPersonId, field)`, delegating to the existing `canEditOwnedRecord` for every governed field except `owner_person_id`, which requires LeadUser+ specifically (stricter than "the record's own owner," a deliberate carve-out) — and `TaskDetailPage.tsx`'s own Owner field switches to this same function too, a real behaviour change to already-shipped Task Detail (a Normal-User owner can reassign Owner there today; after this, they can't), made deliberately for cross-window consistency rather than left as a gap `TaskGrid` alone would have closed. A window's own `columns` prop only ever configures *candidates* — `TaskGrid` still applies `canEditTaskField` (and, for the pre-existing "T" column, the already-existing `isTeamLeadOfAnyTeam`) on top before deciding what actually renders or accepts an edit. Level 1's governed (editable) columns: Status, Tentative Resource Assignment, Priority, Owner, Effort, % Allocation, Task Type, Requestor — Effort Type is deliberately excluded (large knock-on effects on what Effort itself means; stays a Task-Detail-only edit). A cell the current user can't edit is visually greyed (`DenseField.tsx`'s own `READONLY_BG`) before they attempt to edit it, not just silently refused.

- **D1.4-49** (decided 2026-09-18)<br>
  **Question:** `TaskGrid`'s inline editing needs a save mechanism. Task Detail uses staged edits behind an explicit Save button (`D-Win-8`); a grid more naturally wants immediate per-cell commit (edit a cell, it saves). Reconcile the two, or accept the inconsistency for now?<br>
  **Decision:** immediate per-cell save for Level 1 (`TaskGridPlan.md` §4.9, via DataGrid's own `processRowUpdate`), the inconsistency with Task Detail's own model accepted and explicitly recorded, not hidden — reconciling the two (V1.2's own answer was a single "Save All" covering every open edit) is deferred to Level 2, where it needs solving alongside undo anyway (`TaskGridPlan.md` §7, to be added to `Claude/Level2_Implementation/Scope.md`). No new cross-window sync mechanism needed either way — the existing `D-Win-5` `invalidateEverywhere`/`BroadcastChannel` mechanism (`lib/liveSync.ts`) already delivers live updates in both directions (a grid edit reaching an open Task Detail window for the same Task, and a Task Detail Save reaching every open `TaskGrid`) automatically, confirmed by reading that mechanism directly rather than assumed; the only new plumbing is a generic (non-fixed-id) `useUpdateTaskField()` mutation, since the edited row varies per cell edit unlike Task Detail's own fixed-id `useUpdateTask(taskId)`.

- **D1.4-50** (decided 2026-09-18)<br>
  **Question:** where should `TaskGrid`'s own design be documented, and how should All Tasks migrate onto it without risking a regression the user can't easily catch?<br>
  **Decision:** a new `TaskGridPlan.md` (this folder), matching the `PlanViewDesign.md`/`ProjectDetailPlan.md` precedent — not `UserInterfaceWindows.md`, whose own stated purpose (§1 there) is cataloguing V1.2 as it is, not documenting V2's own new components. `V2/UserDocumentation/AllTaskView.md` gets updated once `TaskGrid` is actually built and shipped, per the standing `Plan.md` §7.3 note, not as part of this design. For the migration itself: the current `TaskListPage.tsx` is renamed to `AllTaskOrigPage.tsx` with zero logic changes, moved to `/tasks-orig` behind a temporary second nav-bar button; a new `AllTaskPage.tsx`, built on `TaskGrid`, takes over `/tasks` immediately (not held back until approved) so the two are directly comparable side by side; `AllTaskOrigPage.tsx`, its route, and the temporary button are deleted once the user confirms the two are equivalent.

- **D1.4-51** (decided 2026-09-18)<br>
  **Question:** at the user's request, `Requirements/UserInterfaceWindows.md` §3.20 was written up documenting `V1.2`'s own real `GridControl`/Task Grid implementation, found in the same source sweep as `D-Win-18` there. It found `GUITaskColumns.ColumnIsReadOnly` restricts "T" (Tentatively-Assigned Resources) exactly like Owner — read-only for anyone who isn't a SuperUser/PowerUser (V2: TeamLeadUser/LeadUser), even the Task's own owner — while `TaskGridPlan.md` §4.4 (`D1.4-48`, written before this sweep) had given "T" the same general owner-or-TeamLeadUser rule as every other governed column. Fold in V1.2's own precedent, or keep the two rules as independently arrived at?<br>
  **Decision:** fold it in — `canEditTaskField` (`TaskGridPlan.md` §4.3) now gates `tentative_resource_assignment` identically to `owner_person_id` (LeadUser+ specifically), not the general rule every other governed column uses. Cross-referenced from `UserInterfaceWindows.md`'s own `D-Win-18`.

- **D1.4-52** (decided 2026-09-18)<br>
  **Question:** comparing the V2 `TaskGrid` design against V1.2's own real `GUITask.IsReadOnly` (`UserInterfaceWindows.md` §3.20) surfaced two further gaps beyond "T" (`D1.4-51`): (1) V1.2 lets a Task's own assigned Resource edit Status and Detailed Description even when they're not the Task's owner (`NormalUserEditableColumns`) — `TaskGridPlan.md` §4.3 (`D1.4-48`) had no such tier, only owner-or-LeadUser+; (2) V1.2 also restricts which Status *values* a non-full-rights editor can set a Task to — Closed and Cancelled are removed from the option list entirely (`GUITaskColumns.AdjustComboEditor`), not just gated at the field level. Adopt both into V2, or leave V2 stricter than V1.2 here?<br>
  **Decision:** adopt both. `canEditTaskField` (`TaskGridPlan.md` §4.3) gains a third tier: an `isAssignedResource` parameter, caller-supplied (not looked up inside `lib/permissions.ts` itself, matching how `ownerPersonId` is already caller-supplied) — when true, `status` and `detailed_description` are editable even for a non-owning, sub-LeadUser Resource. A new `editableTaskStatusValues(person, teamId, ownerPersonId)` returns the full `TASK_STATUSES` list for a full-rights editor (owner or TeamLeadUser+) and the list minus `"Closed"`/`"Cancelled"` otherwise, used both by `TaskGrid`'s own Status column editor and by `TaskDetailPage.tsx`'s Status dropdown. `TaskDetailPage.tsx`'s Status and Detailed Description fields switch from the blanket `canEdit` to `canEditTaskField(..., "status"/"detailed_description")` — a real new capability (a non-owning assigned Resource can now use Task Detail at all, for these two fields, where the whole form was previously locked for them), not just a `TaskGrid`-side change. Detailed Description joins Level 1's editable-column set (now nine: Status, Tentative Resource Assignment, Priority, Owner, Effort, % Allocation, Task Type, Requestor, Detailed Description). Full detail in `TaskGridPlan.md` §4.3/§4.4.

- **D1.4-53** (decided 2026-09-18)<br>
  **Question:** V1.2 supports deleting a Task (owner or SuperUser, per `Permissions.IsAllowed`); V2 currently has no way to delete a Task at all — no server endpoint, no client hook, no button anywhere. Should Level 1 add this, and with what permissions?<br>
  **Decision:** yes, added to Level 1 scope (not deferred). Permission is a plain `canEditOwnedRecord(person, teamId, ownerPersonId)` — confirmed, by direct comparison, to already match V1.2's own owner-or-SuperUser rule exactly (V2's TeamLeadUser is V1.2's SuperUser here; no PowerUser-equivalent carve-out exists for deletion in either version). Available from two places: a new trash icon per row in `TaskGrid` itself, and a new header Delete button on `TaskDetailPage.tsx` (mirroring `ProjectDetailPage.tsx`'s own existing Delete button). Both require confirmation via `window.confirm()` before proceeding — explicitly mandatory, not optional. New `DELETE /task/{task_id}` server endpoint (mirroring `DELETE /project/{project_id}`'s own shape), and a new generic `useDeleteTask()` client hook (id supplied per call, the same shape as `useDeleteProject()`). Full detail in `TaskGridPlan.md` §4.10/§6.

- **D1.4-54** (decided 2026-09-19)<br>
  **Question:** after `TaskGrid` shipped, the user reported a real, felt delay (~1.5s estimated) before an already-open Task Detail window reflected a `TaskGrid` edit to the same Task, and asked for a same-machine "fast track" that skips a second trip to the database — explicitly scoped to responsiveness on one user's own machine, not fast propagation to other users on other machines (out of scope, would need a genuine server-push channel instead). What's the fastest same-machine mechanism, and should it replace `D-Win-5`'s existing `invalidateEverywhere`?<br>
  **Decision:** add a second mechanism, `updateEverywhere` (`lib/liveSync.ts`), alongside `invalidateEverywhere` rather than replacing it. `invalidateEverywhere` only ever broadcasts the changed query key, forcing every receiving window (even on the same machine) into a second full network round trip to re-fetch it — `updateEverywhere` instead broadcasts the mutation's own response (the record's fresh value, already in memory) over the same `BroadcastChannel`, and every window applies it directly via `queryClient.setQueryData`, both to the single-record cache entry and, patched in place, the matching row of the bulk list cache entry — no re-fetch anywhere on the receiving side. Wired into `useUpdateTask`, `useUpdateTaskField`, and `useUpdateProject` (the three "one already-known updated record" mutations); every structural mutation (create/delete/assign/unassign/dependencies/remarks/attachments) keeps using `invalidateEverywhere` unchanged. Verified via headless CDP with `Network` domain tracing: an already-open Task Detail window now makes zero network requests when a `TaskGrid` edit to its own Task lands elsewhere. This fix is confirmed working on its own terms, but does *not* fully resolve what the user felt — further timing isolated the dominant cost to something else entirely: a raw `PATCH /task/{id}` against the real `rest-api` is itself intermittently slow (22ms–1224ms across 5 successive calls, vs. a consistent 20–30ms for `GET` on the same endpoint), with connection pooling already ruled out as the cause. Flagged as a distinct, still-open problem (`TaskGridPlan.md` §9, item 4) — not something `updateEverywhere` can paper over, since the broadcast only ever fires once the original mutation itself resolves.

- **D1.4-55** (decided 2026-09-19)<br>
  **Question:** immediately following D1.4-54, the user asked whether the broadcast could fire *before* the PATCH rather than after it, reasoning that a real (non-localhost) network's PATCH latency will be both slower and less predictable than what D1.4-54's own investigation already found even on the local machine — could this decouple propagation speed from PATCH duration entirely?<br>
  **Decision:** yes — the standard optimistic-update pattern. `lib/liveSync.ts` gained `beginOptimisticUpdate`, called from the same three mutations' own `onMutate` (before `mutationFn` fires): cancels any in-flight fetch for the affected queries, reads whatever's currently cached, and — if found — immediately applies + broadcasts a guessed record (cached value overlaid with the fields being changed) via the existing `updateEverywhere`, returning a pre-edit snapshot. `onSuccess` reconciles the guess with the server's own authoritative value; `onError` broadcasts the snapshot back out, rolling the guess back everywhere, not just in the window that made the edit. Trade-off stated to the user up front and accepted: for the (normally brief) window between the guess and its confirmation, every open window shows an unconfirmed value, and a failed mutation means every window briefly shows the wrong value before flipping back — standard optimistic-UI behaviour, not a new class of risk. Verified via headless CDP: a shared-wall-clock console-log trace (not each tab's own `performance.now()`, whose origin differs per page load — a mistake caught before trusting the numbers) showed the broadcast reaching an already-open Task Detail window in 14–51ms regardless of the underlying PATCH's own duration; a rollback test using CDP's `Fetch` domain (a plain in-page `window.fetch` override was tried first and silently failed to intercept anything, since `openapi-fetch` holds its own captured reference to the native `fetch` from module load) confirmed the optimistic guess appears immediately and then correctly rolls back everywhere when the PATCH is made to fail, with the server itself left untouched. Full detail, including the methodological note on why naive CDP-harness wall-clock timing gave misleadingly large numbers earlier in this same investigation, in `TaskGridPlan.md`'s own D1.4-55 write-up.

- **D1.4-56** (decided 2026-09-19)<br>
  **Question:** the user asked for V1.2's own right-click grid menu (Reset All Filters / Copy All / Show Filter) to be reproduced on `TaskGrid`, pointing at the V1.2 source itself for the exact behaviour rather than a guess. What does V1.2's real `GridControl` actually do, and does it reveal anything `TaskGrid`'s own existing filter-visibility design got wrong?<br>
  **Decision:** reproduced all three, read directly from `V1.2/Libs/CustomGUIControls/CustomGUIControls/Grid/GridControl.cs`/`.Designer.cs`. The menu attaches to the whole grid (not just its filter row), matching V1.2's own `dataGridView.ContextMenuStrip`. "Reset All Filters" clears `filterState`; "Copy All" copies every visible row/column as tab-separated text (header row first, current sort order, each column's own *displayed* text via `apiRef.current.getCellParams(...).formattedValue`, excluding the `__delete` actions column — `TaskGrid`'s equivalent of V1.2's own excluded hidden "column zero") to the OS clipboard; "Show Filter" toggles the filter row's own visibility with a check-mark reflecting its state. Reading V1.2's `ApplyFiltersToRows` directly surfaced a genuine gap in `TaskGrid`'s own original design: V1.2's filtering has always been independent of the filter row's own visibility (hiding the boxes never drops an already-set filter), but `TaskGrid`'s `showFilters` prop was an all-or-nothing switch that also disabled filtering entirely when `false`. Fixed to match V1.2: `showFilters` is now only the *initial* value of a runtime-togglable `filterVisible` state, and `passesAllFilters` no longer short-circuits on it at all. Verified via headless CDP: the three menu items appear in the right order; toggling "Show Filter" never changes which rows are visible; "Reset All Filters" reveals previously-hidden Closed/Cancelled Tasks; "Copy All" (clipboard permissions granted via `Browser.grantPermissions` for the headless test) produces a correctly-shaped payload with real display text. Full detail in `TaskGridPlan.md` §4.11/§4.6.

- **D1.4-57** (decided 2026-09-19)<br>
  **Question:** the user reported that on Task #10990, its actually-assigned Resource ("Friedel") doesn't appear at all in Task Detail's own Resources checklist — checked against the real data, Friedel holds a `person_role` on the Task's Team with `is_resource: false` (he'd been assigned as a Resource before that changed, or was never a formal Team Resource despite the assignment existing). `resourceCandidates` (`TaskDetailPage.tsx`, D1.4-18/21/22) only ever lists people who currently pass `is_active && is_resource-on-this-Team`, so anyone assigned outside that set is invisible in the checklist — with no way to see them, let alone unassign them, from this screen at all. Should the checklist candidate set change?<br>
  **Decision:** yes — the Resources checklist must show *everyone currently assigned*, even someone no longer a valid candidate, precisely so a Lead User can see and remove them if they choose; once removed, they're gone from the list for good, since they're still not a genuine candidate (this was the user's own explicit reasoning, confirmed as intended). Added `staleAssignedPeople` (people in the staged `resourceIds` set but absent from `resourceCandidates`, pulled from the full unfiltered `people` list) and folded it into a new `resourceListCandidates` used only by the Resources checklist (`sortedResourcePeople`) — deliberately not into `resourceCandidates` itself, which the Owner dropdown (`sortedOwnerPeople`) also reads and must keep its own stricter "genuine current Resource" set, since reassigning Owner *to* someone in this situation was never part of the ask. Verified against the real reported case: Friedel (person 10005, `is_resource: false` on the Task's Team, but present in `task_resource` for Task #10990) now appears in the checklist, checked, sorted first by the existing checked-first ordering — and since he's surfaced only via being currently assigned, unchecking him removes him from the list on the next load, exactly as intended.

- **D1.4-58** (decided 2026-09-20)<br>
  **Question:** the user asked for two new, recursively-composed GUI components — `Project` (singular: name, a `TaskGrid` for its own Tasks, then a list of further `Project`s for its sub-Projects) and `Projects` (plural: a None/Open/All Task-visibility toggle, then a list of `Project`s) — matching V1.2's own real `ProjectControl`/`ProjectStackControl` structure. Since `Projects` owns its own toggle, naively recursing through another `Projects` instance for a `Project`'s own sub-Projects would duplicate that toggle at every level, which V1.2 never does (exactly one toggle, on the outermost window). How should the recursion avoid that?<br>
  **Decision:** give `Projects` a `showToggle` prop (default `true`), and have `Project`'s own sub-Project section instantiate `Projects` again with `showToggle={false}` and the inherited `taskVisibility` passed straight through, rather than a plain inline list duplicating `Projects`'s own sort/render logic. Chosen by the user specifically to minimise code and the risk of the inline-list copy drifting from `Projects`'s own real logic over time. Full detail in `ProjectsGUIComponent.md` §4.3.
- **D1.4-59** (decided 2026-09-20)<br>
  **Question:** embedding a `TaskGrid` per `Project` (`D1.4-58`) surfaced two gaps — `TaskGrid.tsx` hard-codes a fixed 600px height (`AllTaskPage`-appropriate, absurd for a small embedded grid), and always shows its "1–100 of 100" pagination footer even when every row already fits on one page. Should these fixes be scoped to embedded instances only, or applied everywhere `TaskGrid` is used?<br>
  **Decision:** everywhere, including `AllTaskPage` — confirmed as a deliberate, welcome behaviour change to that already-shipped page too, not an embedded-only special case. `TaskGrid` always uses DataGrid's own `autoHeight` (sizes to its own content, no internal scrollbar) and always hides its own footer whenever `filteredTasks.length <= 100` (computed from its own current row count, not a caller-supplied flag) — the footer, and its pagination, only ever appears once there's genuinely more than one page's worth of Tasks. Full detail in `ProjectsGUIComponent.md` §4.5.
- **D1.4-60** (decided 2026-09-20)<br>
  **Question:** should `Project`/`Projects` be built by embedding a `TaskGrid` inside an `@mui/x-tree-view` `TreeItem` (today's `ProjectTaskTree.tsx` is already built on `SimpleTreeView`/`TreeItem`, `D1.4-1`), or as bespoke recursive components with no tree widget at all?<br>
  **Decision:** bespoke recursive components (no `@mui/x-tree-view`) — `@mui/x-tree-view` owns its own keyboard/focus/selection model across every `TreeItem`, and `@mui/x-data-grid` owns an equally real, different one of its own; nesting one inside the other means two widgets both trying to own arrow-key behaviour in the same DOM subtree, a structural mismatch rather than a matter of careful implementation. `D1.4-1`'s own choice of `@mui/x-tree-view` stays correct for other tree-like browsing, if any arises — this doesn't revisit that decision generally, only for this one case, where the user's own described design (name, then a grid, then a list of more of the same) was already a plain block layout, not a tree-selection widget, before this decision. Full detail in `ProjectsGUIComponent.md` §6.

- **D1.4-61** (decided 2026-09-20)<br>
  **Question:** `Q1.4-61` — should `Project`'s own per-row actions close the gap V1.2 has closed at every depth (V2 today only offers "Add New Project"/"Add Task" once, for the currently-open Project, above the tree, `ProjectsGUIComponent.md` §4.6)?<br>
  **Decision:** yes, for "Add Task" only — a per-row `onAddTask` at every depth, matching V1.2's own `imageAddTask` (present on every `ProjectControl`, `ProjectsGUIComponent.md` §2.1) exactly. Re-checking `ProjectControl.xaml`/`.xaml.cs` directly (prompted by the user's own recollection, confirmed correct) found V1.2 has **no** per-row "Add (Sub-)Project" affordance anywhere — only "Add Task" and a Delete/Rename context menu. Creating a new Project is therefore not part of `Project`'s own design at all; it stays exactly as it already is, a page-level action on whichever window is open (`ProjectDetailPage.tsx`'s existing "Add New Project" button), unaffected by this work. Full detail in `ProjectsGUIComponent.md` §4.6.
- **D1.4-62** (decided 2026-09-20)<br>
  **Question:** `Q1.4-62` — now that `Project`'s real design exists, is `D1.4-42`'s deferral of a per-row task-count/priority summary (V1.2's `"(High - 4/12)"` label) to Stage 6 Polishing still the right call, or should it be pulled forward into this work?<br>
  **Decision:** still deferred — no change to `D1.4-42`'s own scope. The user wants to give this more thought specifically when Stage 6 Polishing is actually reached, not decide it now alongside the rest of this design.

- **D1.4-64** (decided 2026-09-20)<br>
  **Question:** while checking implementation-readiness for `Project`/`Projects`, re-checking `ProjectTaskTree.tsx`'s existing `canManageTeam` callback (which gates both its rename and delete icons) against what `rest-api/app/routes/projects.py` actually enforces found neither action's real server rule matches it: rename needs `require_owner_or_team_lead` (`canEditOwnedRecord(person, teamId, project.owner_person_id)`), delete needs `is_team_lead` only (stricter, no owner exception) — `canManageTeam`'s own `hasRoleAtLeast(person, teamId, "LeadUser")` matches neither, and ignores the Project's own owner entirely for rename. Should this be fixed as part of specifying `Project`'s own new permission-checking, and if so, how?<br>
  **Decision:** yes, confirmed by the user. Fixed by removing permission-check props from `Project`/`Projects` entirely, rather than just correcting `canManageTeam`'s formula in the same injected-callback shape — extending `D1.4-48`'s own centralising principle for `TaskGrid` ("never a per-window override... used identically by every window") to `Project`: it calls `useAuth()` and `lib/permissions.ts`'s existing, already entity-agnostic `canEditOwnedRecord`/`isTeamLead`/`hasRoleAtLeast` directly, per row, from its own `project` prop — the same way `TaskGrid` already computes its own field-level permissions internally rather than trusting each embedding window to re-derive and inject the right check, which is exactly how this bug happened in the first place. `onRenameProject`/`onDeleteProject`/`onAddTask` stay as injected callbacks (which dialog opens is still page-specific); only whether the icon shows at all moves out of the embedding window. `ProjectDetailPage.tsx`'s own header Delete button and "Add New Project"/"Add Task" buttons were independently re-checked and already match the server exactly — unaffected, only the tree's own per-row icons were wrong. Full detail in `ProjectsGUIComponent.md` §4.6.
- **D1.4-65** (decided 2026-09-20)<br>
  **Question:** `TaskGridPlan.md` §5.4's own cleanup plan for `AllTaskOrigPage.tsx` (the comparison baseline kept alongside `AllTaskPage.tsx` since `D1.4-50`) was conditional on the user confirming the two are equivalent. Confirmed?<br>
  **Decision:** yes — `AllTaskOrigPage.tsx`, its `/tasks-orig` route, and the temporary "Tasks (orig)" nav-bar button are approved for removal, per `TaskGridPlan.md` §5.4's own already-specified plan. Not yet executed (documentation-only turn, per the user's own "no changes yet") — full detail in `TaskGridPlan.md` §5.4.
- **D1.4-66** (decided 2026-09-21)<br>
  **Question:** designing Component Detail (`ComponentDetailPlan.md`), does Component's own delete permission match Project's (`isTeamLead` only, `D1.4-64`'s own table), or something else?<br>
  **Decision:** something else — reading `rest-api/app/routes/components.py` directly found `delete_component` uses `require_owner_or_team_lead`, the *same* check as its own `update_component` (and as Project's own rename rule), not the stricter TeamLead-only rule Project's delete uses. A real, confirmed difference between the two entities, not an assumption — Component's own `canDelete` is `canEditOwnedRecord(...)`, not `isTeamLead(...)`. Full detail in `ComponentDetailPlan.md` §2.3.
- **D1.4-70** (decided 2026-09-21)<br>
  **Question:** Component Detail's own browsing tree (`ComponentDetailPlan.md` §5.2) — a new, parallel `Component`/`Components` pair mirroring `Project`/`Projects`'s own conventions (Option A), or generify `Project`/`Projects` into one pair parameterised over Project vs. Component (Option B)?<br>
  **Decision:** Option A, confirmed by the user. Matches the precedent already set for `TaskGrid` vs. `Project`/`Projects` themselves — `TaskGrid` was centralised because every window shows the *same* Task shape and the *same* editability rules, but Project and Component are two genuinely different entities with two genuinely different rule sets (`D1.4-66`, sibling ordering, the "only active" filter, the embedded column set) that happen to share a tree shape; forcing them through one parameterised component risks exactly the kind of subtle cross-entity bug `D1.4-64` already found once. Full detail in `ComponentDetailPlan.md` §5.2.
- **D1.4-71** (decided 2026-09-21)<br>
  **Question:** adding a Task from a Component (`ComponentDetailPlan.md` §4.6) needs a Project picker where the existing `AddTaskDialog.tsx` (opened from a Project) instead pre-fills the Project and offers a Component picker — generalise the existing dialog, or build a separate one for this case?<br>
  **Decision:** generalise the existing dialog, confirmed by the user — an `openedFrom: { kind: "project"; ... } | { kind: "component"; ... }` discriminator, swapping which of Project/Component is implicit vs. picked. Keeps one dialog rather than two copies of the same validation/error-handling drifting apart, the same reasoning `D1.4-64`'s fix was built on. Full detail in `ComponentDetailPlan.md` §4.6.
- **D1.4-72** (decided 2026-09-21)<br>
  **Question:** the Component-rooted Gantt Display `Plan.md` §6.3 already commits to (`ComponentDetailPlan.md` §4.8) — build it as part of the same slice of work as Component List/Detail, or defer it as its own explicit follow-up?<br>
  **Decision:** deferred, confirmed by the user — Component List/Detail itself lands and is verified first; the Gantt Display extension (`lib/ganttLayout.ts`/`PlanPage.tsx` gaining a Component-rooted tree walk alongside the existing Project-rooted one) is its own separate follow-up. Full detail in `ComponentDetailPlan.md` §4.8.
- **D1.4-73** (decided 2026-09-22)<br>
  **Question:** while designing Search/Find (`SearchPlan.md`), the user observed that V1.2's own Find results grid reuses the *same* generic grid engine every other V1.2 window (Task/Project/Component) already shares — should V2's `TaskGrid.tsx` similarly become (or share a base with) a more generic grid, now that Search needs a second, differently-shaped grid consumer?<br>
  **Decision:** extract `TaskGrid.tsx`'s generic dense-grid *chrome* (sizing constants, text measurement/auto-sizing, border/header styling, the right-click context menu) into a new shared `components/DenseDataGrid.tsx`, used by both `TaskGrid` and Search's own results grid — but do not rebuild a full V1.2-style generic "any entity, any column" grid engine on top of it. Column definitions, cell editability, and Task's own Urgency tinting stay in `TaskGrid` itself; Search's read-only, five-different-record-types results grid builds its own plain columns on the same shared chrome instead of being forced through `TaskGrid`'s Task-specific editing machinery — the same "don't force two genuinely different rule sets through one parameterised component" reasoning `D1.4-70` already established for Component vs. Project. Full detail in `SearchPlan.md` §5.
- **D1.4-74** (decided 2026-09-22)<br>
  **Question:** should Search (`SearchPlan.md` §4.2) re-run live as the user types, or only on an explicit trigger?<br>
  **Decision:** explicit trigger (Enter key or a "Find" button), confirmed by the user — matches V1.2's own `FormFind` model. Unlike TaskGrid's own filter-row inputs (which filter already-loaded rows locally), each Search run is a real network round-trip to `GET /search`, and Level 1 has no need for the added complexity of debouncing a live-search input. Full detail in `SearchPlan.md` §4.2.
- **D1.4-75** (decided 2026-09-22)<br>
  **Question:** `GET /search` (`rest-api/app/routes/search.py`) currently matches its whole query string as one literal substring, not V1.2's own space-separated, ANDed-term behaviour (`user login` should match a row containing both words anywhere, not the literal phrase) — fix this server-side, or leave it and do any extra matching client-side?<br>
  **Decision:** fix it server-side, confirmed by the user — split `q` on whitespace and require every resulting word to match somewhere on the row (AND across words), each word itself OR'd across that type's own multiple searchable columns (mirroring V1.2's `Task.ContainsText`'s own `Description OR DetailedDescription` shape: a word in Description and a different word only in DetailedDescription still matches). Doing this client-side instead was rejected — the browser-side reference data never fetches Attachment's `mail_from` column at all (no hook pulls it down), so client-side AND-matching would have a genuine blind spot the SQL itself doesn't. Full detail in `SearchPlan.md` §6.1.
- **D1.4-76** (decided 2026-09-22)<br>
  **Question:** should Search's results grid (`SearchPlan.md` §4.4) colour-code Task-typed rows by Urgency, matching `Plan.md` §2's own note that any Task-showing screen built from here on should include Urgency "wherever doing so is actually appropriate," or stay plain (V1.2's own Find results predate Urgency as a concept entirely)?<br>
  **Decision:** adopt it, confirmed by the user — Task-typed rows reuse `TaskGrid.tsx`'s own `computeTaskRowColour`/`computeUrgency`-driven per-row tint verbatim; Project/Component/Remark/Attachment rows render untinted (no Urgency concept for those types). Full detail in `SearchPlan.md` §4.4.
- **D1.4-77** (decided 2026-09-22)<br>
  **Question:** Search's results grid (`SearchPlan.md` §4.4) originally skipped per-column filtering (plain `GridColDef`s, no filter row) on the reasoning that the search box/type checkboxes/Include Closed checkbox already narrow the row set — should it instead get the same real per-column filter row, and the full "Reset All Filters"/"Show Filter"/"Hide filter" context-menu set, `TaskGrid` already has?<br>
  **Decision:** yes, confirmed by the user, specifically because this is shared chrome (`components/DenseDataGrid.tsx`'s `useDenseGridColumns`, `D1.4-73`) — a grid opting out of it needs its own justification, and "these four columns are short and predictable" wasn't judged strong enough once the capability already exists for free. Search's four columns (Type/Description/Date/Person) now run through `withFilter` exactly like `TaskGrid`'s own columns, and `DenseDataGrid`'s context menu shows its full set for Search too. Full detail in `SearchPlan.md` §4.4.
- **D1.4-78** (decided 2026-09-22, revised same day)<br>
  **Question:** with Search's columns all auto-sized to fixed pixel widths, the user reported what looked like a genuine extra column — no header, no filter, no data — that stretched as the window resized. This was MUI DataGrid's own "filler" element filling the leftover horizontal space, not a real column. How should the leftover space be handled, and should the user still be able to manually resize every column, including whichever one absorbs it?<br>
  **Decision:** give `useDenseGridColumns`'s `withFilter` (`components/DenseDataGrid.tsx`) an optional `flex` argument, applied unconditionally (never dropped by a manual resize, unlike a plain `width`) and paired with `resizable: false` on that same column — stopping the drag/autosize gesture from ever starting on it, rather than starting it and then visibly snapping back once React re-renders. The *trailing* column is the only safe place for this: a middle `flex` column has a divider on both sides, and disabling its own resize handle also disables the shared boundary with its next-door neighbour, which is what a first attempt (Description, ahead of Date/Person) ran into — reordered to Type/Date/Person/Description so Description is both last and the `flex` column, leaving every other divider (including Description's own left edge, i.e. Date|Person's boundary) freely draggable. Generic chrome, not Search-specific — any future `DenseDataGrid` consumer needing the same "one column fills the rest" behaviour can use the same argument, provided it's the trailing column. Full detail in `SearchPlan.md` §4.4.
- **D1.4-79** (decided 2026-09-22)<br>
  **Question:** MUI DataGrid's own native "double-click the resize separator to autosize" gesture proved unreliable with this app's custom `renderHeader` content (reported as indistinguishable from two ordinary single clicks) — how should double-click-to-autosize actually be implemented, and separately, double-clicking a sortable column's header also fires two ordinary browser `click` events first (unavoidable — a double-click is always "click, click, dblclick"), each independently toggling MUI's own click-to-sort, leaving the grid sorted by whatever column was just resized instead of whatever it was actually sorted by — how should that be prevented without adding a felt delay to ordinary single-click sorting everywhere?<br>
  **Decision:** implement double-click entirely ourselves rather than depending on MUI's own event plumbing for it — a plain `onDoubleClick` directly on `FilterableHeader`'s own label element (`GridColumnFilter.tsx`), which sets that column's width to its own widest-actual-content measurement directly (deliberately ignoring any `widthCap` — TaskGrid's Description/Detailed Description/Ref URL columns are capped only for their *default* width, not when a user explicitly asks to see the full content). For the sort side-effect: rather than debouncing every click to tell "part of a double-click" from "a genuine click" apart, `useDenseGridColumns` keeps a rolling 2-entry log of the sort model from just before each of the last two `sortModelChange` events (via `apiRef.current.subscribeEvent`), and the same double-click handler restores whichever one is two changes back — undoing exactly the two clicks' own toggles, leaving any earlier, real sort choice alone. Requires the caller to create its own `useGridApiRef()` and pass the *same* instance to both `useDenseGridColumns` and `DenseDataGrid` (TaskGrid already had one; Search gained one for this). A pragmatic Level 1 fix, not a fully general one — see `Claude/Level2_Implementation/Scope.md`'s own new item on reconsidering the grid library entirely, prompted directly by how much targeted workaround code (`D1.4-73`, `D1.4-76`, `D1.4-78`, this one) it's taken to get TaskGrid/Search's shared dense-grid chrome working the way this app actually needs.
- **D1.4-80** (found and fixed 2026-09-22)<br>
  **Not a design question — a real, shipped regression from `D1.4-73`'s own chrome extraction, caught only once a user actually typed into a filter box and watched nothing happen.** `useDenseGridColumns`'s `filteredRows` was computed *eagerly*, inside the hook's own body, before the caller had ever called `withFilter` for a single column — meaning `filterConfigs` (only populated *by* those `withFilter` calls) was always empty at the point the filtered set was computed, so every column filter — the mini checklist popover and the plain text box alike — silently did nothing at all, on both `TaskGrid` and Search, for as long as this bug existed.<br>
  **Fix:** `filteredRows` is now `getFilteredRows: () => T[]`, a function the caller invokes explicitly, once, after every `withFilter` call for that render has already run (immediately before rendering `DenseDataGrid`, never any earlier) — both `TaskGrid.tsx` and `SearchPage.tsx` updated to call it at that point instead of destructuring a pre-computed value.
- **D1.4-81** (found and fixed 2026-09-22)<br>
  **Not a design question — a real, shipped crash from the `D1.4-79` sort-preservation work.** `useGridApiRef()` is a plain `useRef(null)` (confirmed by reading MUI's own source) — `.current` only becomes non-null once the real `<DataGrid>` it's attached to actually mounts. Search's own results grid is conditionally rendered (only once a search is submitted), so `useDenseGridColumns`'s sort-history effect — calling straight into `apiRef.current.getSortModel()` on mount — threw with `apiRef.current` still `null`, and with no error boundary anywhere in this app, that blanked the entire Search window to a plain white page on open, before a search was even run.<br>
  **Fix:** the effect now polls (every 200ms, cleaned up on unmount) until `apiRef.current` actually exists, and re-subscribes if the underlying grid instance ever changes (e.g. Search's grid unmounting/remounting across separate searches in the same still-open window) — rather than assuming a `useGridApiRef()` ref is already populated the first time an effect keyed on it runs.
- **D1.4-82** (decided 2026-09-22, after four attempts)<br>
  **Question:** the darkened header-row divider (§ the styling round that produced `borderRight: "...!important"` on `FilterableHeader`'s label Box, then two further attempts — an absolutely-positioned overlay sized to the label's own height, then to `100%` of an explicit-height root Box) kept failing the exact same way: visible with the filter row hidden, gone the moment it was shown — confirmed not a caching artefact (reproduced in a fresh Incognito window, and after unregistering the service worker). What was actually going on, and what's the fix?<br>
  **Decision:** a user's own DevTools inspection (clicking exactly on the divider's own position with the filter shown) settled it directly — the element actually sitting there was MUI's own native `.MuiDataGrid-columnSeparator` (its built-in resize-handle/separator), not anything `colDef.renderHeader` produces. That element lives in its own stacking context, one level above anything our own custom header content can reach — no `z-index` set from *inside* `FilterableHeader`'s own output can ever out-rank it, which is why three different overlay-based attempts each failed for reasons that resisted diagnosis from source code alone. The actual fix doesn't fight that element at all: MUI's own header-cell border reads its color from the CSS custom property `--DataGrid-rowBorderColor`, and overriding it *only* on the header's own container (`.MuiDataGrid-columnHeaders`, confirmed from MUI's own source to be a separate sibling from the data rows' own container, so the main grid's borders are untouched) recolours the real, already-painted border directly — nothing new layered on top, nothing to lose a stacking fight. `GridColumnFilter.tsx`'s custom divider `Box` (and the explicit root-box height it needed) was removed entirely; `DenseDataGrid.tsx`'s base `sx` sets the variable instead. Confirmed fixed by the user with the filter both shown and hidden. The practical lesson for this codebase, worth remembering before the next MUI DataGrid visual customisation: check for an official CSS variable/theming hook first, since a custom element competing with MUI's own internal overlays (separators, resize handles, menus) for stacking order is fragile in ways that are genuinely hard to diagnose without live DevTools access.
- **D1.4-83** (decided 2026-09-23)<br>
  **Question:** Manage People's own core UX — should Create Person's own password ever be set by the Person themselves (a forced first-login change), should the People grid get per-column filtering given Level 1's small volumes, and should an organisation admin be able to remove their own `is_organisation_admin` flag?<br>
  **Decision:** password stays fully admin-driven — one dialog, used both to set a brand-new Person's first password and to reset an existing one later; the admin communicates it out of band, and the Person has no ability to set or change it themselves at all for Level 1 (no forced first-login step either — that's a genuine Level 2 question, alongside self-service change and forgotten-password recovery generally, not decided here). Filtering: yes, included, hidden by default (matching Search's own `D1.4-77` precedent) — an admin People list is exactly the kind of thing that's small today and grows over time. Self-demotion: guarded, both server-side (`update_person` rejects a caller unsetting their own flag) and client-side (that checkbox disabled on your own row) — client-only would never be sufficient for something this sensitive. Full detail in `ManagePeoplePlan.md` §4.2/§4.4/§4.6.
- **D1.4-84** (decided 2026-09-23)<br>
  **Question:** should Person deletion come back at all, given `2_RestApi/Plan.md`'s own Level 1 build deliberately has no delete route for Person ("People are never hard-deleted")?<br>
  **Decision:** yes, narrowly — see `Requirements/DomainModel.md`'s own `D-DM-12` for the full reasoning (a real `DELETE /person/{id}`, `is_organisation_admin`-only, rejected unless the Person is referenced *nowhere at all* across every table that can reference one, including `PersonRole` itself). Recorded here too since it's this screen's own delete button/confirmation flow that surfaces it, not just the server route. Full detail in `ManagePeoplePlan.md` §4.5/§6.
- **D1.4-85** (decided 2026-09-23)<br>
  **Question:** Team Management (a Team Lead managing their own Team's membership, role, `is_resource`, and nickname) was originally scoped to Level 2 (`D-DM-11`) pending "a dedicated Team Management screen." Is it needed for Level 1 after all, and if so, how does it relate to Manage People?<br>
  **Decision:** yes, needed for Level 1 — folded into this same piece of work rather than planned separately. A distinct screen from Manage People (different audience — `is_organisation_admin` *or* `is_team_lead(team_id)` for the specific Team, not org-admin-only — and different data scope, one Team at a time rather than every Person org-wide), reusing `ProjectDetailPage.tsx`'s own no-id/with-id duality: `/team-management` resolves to a Team picker (every Team, for an admin; just the Teams they lead, for a Team Lead with more than one) or redirects straight to the one Team a single-Team Lead leads, with no extra click; `/team-management/:teamId` shows a switcher only when the viewer genuinely has more than one Team available to switch to. Full detail in `ManagePeoplePlan.md` §5.
- **D1.4-86** (decided 2026-09-23)<br>
  **Question:** three smaller Team Management questions surfaced while designing it — should removing a Person from a Team be blocked if they still own/are assigned things on it (mirroring Person deletion's own hard reference-check), should a *different* admin deactivating the only other remaining admin be guarded against, and where do the growing number of admin-only nav buttons (Manage People, Team Management, and the still-unbuilt Admin tooling screen, alongside the existing "Admin" chip) live in `AppShell.tsx`?<br>
  **Decision:** removing a Team member is a soft warning, not a hard block — naming what's still attached on that Team if anything, but letting the removal proceed once confirmed regardless, since (unlike deleting a Person) nothing is actually at risk of an orphaned reference. The last-remaining-admin gap is left unguarded for Level 1, matching V1.2's own equivalent top tier never having this guard either, and it stays recoverable via direct DB access — noted here explicitly rather than left unrecorded. Nav button placement is deliberately not decided now — a real question once Admin tooling exists too, likely resolved by grouping them then rather than pre-emptively here. Full detail in `ManagePeoplePlan.md` §5.4/§4.6/§9.
- **D1.4-87** (decided 2026-09-23)<br>
  **Question:** Team Management (`D1.4-85`) needs two server-side capabilities neither built nor previously decided: editing a Team member's nickname (`D-DM-11`'s own field, left read-only pending this exact screen), and something enforcing `teams.py`'s own stated-but-unenforced invariant that "a Team is never left leaderless" beyond the moment of Team creation.<br>
  **Decision:** both added. `nickname` joins `is_resource`/`role` on `WritePersonRoleRequest`/`UpdatePersonRoleRequest` (`D-DM-11` amended accordingly — no longer Level 1 read-only). A new never-leaderless guard rejects (400) any `PATCH`/`DELETE` on `PersonRole` that would leave its Team with zero remaining `TeamLeadUser` rows — covering both a lone Team Lead demoting/removing themselves (naturally blocked, no separate self-check needed the way Manage People's own admin-flag guard required, `D1.4-83`) and removing/demoting the last of several. Full detail in `ManagePeoplePlan.md` §5.5/§5.6/§6.

See `../ImplementationPlan.md` for how this phase fits into the Level 1 plan, and for open questions that span this phase and others.
