First, read all of the documents under the folder 'Claude/Guidelines'

We are designing a new implement for ProjectPal.

The previous implementation for ProjectPal can be found in the folder 'V1.2'

The new implementation for ProjectPal is in the folder 'V2'.

The goals for the new implementation can be found here Claude/Requirements/Goals.md - read this document.

Before we can create the new implementation we need to specify many aspects of it.

The domain model is in the document Claude/Requirements/DomainModel.md

The key concepts are in the document Claude/Requirements/KeyConcepts.md

The use case are in the document Claude/Requirements/UseCases.md

We need to flesh out all of these documents and probably some more too.

